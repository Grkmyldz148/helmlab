\documentclass[11pt, twocolumn]{article}

% ── Packages ──────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{mathptmx}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage[margin=2cm]{geometry}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{caption}
\usepackage{subcaption}

\hypersetup{colorlinks=true, linkcolor=blue!60!black, citecolor=blue!60!black, urlcolor=blue!60!black}

\graphicspath{{figures/}}

% ── Shortcuts ─────────────────────────────────────────────────────
\newcommand{\helmlab}{\textsc{Helmlab}}
\newcommand{\stress}{\textsc{stress}}
\newcommand{\combvd}{\textsc{combvd}}
\newcommand{\ie}{i.e.,\,}
\newcommand{\eg}{e.g.,\,}
\newcommand{\etal}{\textit{et al.}}

% ══════════════════════════════════════════════════════════════════
\title{\helmlab: A Data-Driven Analytical Color Space\\for UI Design Systems}

\author{
  G\"orkem Y{\i}ld{\i}z\\
  \textit{Independent Researcher}\\
  \texttt{author@gorkemyildiz.com}
}

\date{}

% ══════════════════════════════════════════════════════════════════
\begin{document}
\maketitle

% ── Abstract ─────────────────────────────────────────────────────
\begin{abstract}
We present \helmlab, a 72-parameter analytical color space for
UI design systems. The forward transform maps CIE~XYZ to a
perceptually-organized Lab representation through learned matrices,
per-channel power compression, Fourier hue correction, and embedded
Helmholtz--Kohlrausch lightness adjustment. A post-pipeline neutral
correction guarantees that achromatic colors map to
$a\!=\!b\!=\!0$ (chroma $<10^{-6}$), and a rigid rotation of the
chromatic plane improves hue-angle alignment without affecting the
distance metric, which is invariant under isometries. On the
\combvd\ dataset (3{,}813 color pairs), \helmlab\ achieves a
\stress\ of 23.30, a 20.2\% reduction from CIEDE2000 (29.18).
A blue-band refit with sub-dataset penalties reduces gradient
non-uniformity in the blue--cyan region by $8.9\times$ at a cost
of only $+0.08$ \stress.
Cross-validation on He~\etal\ 2022 and MacAdam~1974 shows
competitive cross-dataset performance. The transform is
invertible with round-trip errors below $10^{-14}$.
Gamut mapping, design-token export, and dark/light mode
adaptation utilities are included for use in web and mobile
design systems.
\end{abstract}

\textbf{Keywords:} color space, color difference, perceptual uniformity, STRESS, Helmholtz--Kohlrausch, UI design systems


% ══════════════════════════════════════════════════════════════════
\section{Introduction}
\label{sec:intro}

UI design systems need accurate perceptual distance prediction for
contrast checking, an achromatic axis where grays map to zero chroma
for gradient interpolation, and reasonable hue-angle alignment for
programmatic color manipulation. No existing color space provides all
three.

CIE~Lab~\cite{cie1976} and CIEDE2000~\cite{luo2001} are the
standard for color-difference evaluation, but their
\stress\ on \combvd\ is 29.18.
Oklab~\cite{ottosson2020} is simple and part of CSS Color Level~4,
but was optimized for hue uniformity rather than color-difference
prediction, and gets \stress\ 47.5 (Euclidean) on \combvd.
CAM16-UCS~\cite{li2017} is derived from a color appearance model
but is computationally expensive and has achromatic chroma leakage
($\bar{C}=1.42$). IPT~\cite{ebner1998} was optimized for hue
linearity at the expense of lightness accuracy.
$J_z a_z b_z$~\cite{safdar2017} targets HDR via the PQ curve but
is complex and not CSS-native. None of these spaces jointly
optimize the color-space transform and distance metric end-to-end
against psychophysical data.

In this paper, we treat the entire XYZ-to-distance pipeline as a
single system with 72~learnable parameters and optimize all of them
jointly against the largest available color-difference dataset.
The contributions are:

\begin{enumerate}
  \item A 72-parameter analytical color space with \stress\ 23.30
    on \combvd, 20.2\% lower than CIEDE2000.
  \item A neutral correction that guarantees $C < 10^{-6}$ for
    achromatic colors, at a \stress\ cost of only $+0.04$.
  \item A rigid rotation in the $ab$ plane that reduces hue error
    (RMS~18.1\textdegree) without changing the distance metric
    (the metric is invariant under rotation).
  \item Embedded Helmholtz--Kohlrausch correction where lightness
    depends on chroma, with parameters learned from data.
  \item A blue-band refit that reduces gradient non-uniformity
    in the blue--cyan region by $8.9\times$ while improving
    \stress\ on 5 of 6 \combvd\ sub-datasets.
  \item Gamut mapping, CSS/Android/iOS token export, and dark/light
    mode adaptation for design system use.
\end{enumerate}


% ══════════════════════════════════════════════════════════════════
\section{Design Goals and Metrics}
\label{sec:goals}

\helmlab\ is optimized for UI design systems operating in sRGB and
Display~P3 gamuts under typical viewing conditions (D65 illuminant,
average surround). We define five quantitative targets:

\paragraph{Color-difference accuracy (\stress).}
The STRESS metric~\cite{luo2006} measures agreement between predicted
distances $\Delta E_i$ and visual differences $DV_i$:
\begin{equation}
  \mathrm{STRESS} = 100\sqrt{\frac{\sum_i(DV_i - F \cdot \Delta E_i)^2}{\sum_i DV_i^2}}
  \label{eq:stress}
\end{equation}
where $F = \sum DV_i \Delta E_i / \sum \Delta E_i^2$. Lower is better.
We train on \combvd\ (3{,}813 pairs from six experiments~\cite{luo2006})
and cross-validate on He~\etal\ 2022~\cite{he2022} (82~pairs) and
MacAdam~1974~\cite{macadam1974} (128~pairs).

\paragraph{Achromatic integrity.}
Neutral colors (D65-adapted grays, \ie\ stimuli on the achromatic axis
proportional to the D65 white point) must map to $a = b = 0$. We
measure maximum chroma $C_{\max}$ over 256 gray levels spanning
$Y \in [0.001, 2.0]$.

\paragraph{Hue-angle alignment.}
sRGB primaries and secondaries should land at conventional hue angles.
We report RMS and maximum hue error across
$\{$R, Y, G, C, B, M$\}$.

\paragraph{Round-trip accuracy.}
For any sRGB color, $\|T^{-1}(T(\mathbf{x})) - \mathbf{x}\|_\infty$
should be below $10^{-8}$.

\paragraph{Munsell uniformity.}
Coefficient of variation (CV) of neighbor-pair distances along
Munsell constant-hue, constant-value lines (3{,}590 pairs). Lower CV
indicates more uniform spacing.


% ══════════════════════════════════════════════════════════════════
\section{The Helmlab Color Space}
\label{sec:space}

\subsection{Forward Transform}

The forward transform maps CIE~XYZ (D65) to Lab through eleven stages
(\Cref{fig:pipeline}).
All parameters are jointly optimized (\Cref{sec:optimization}).

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig4_pipeline.pdf}
  \caption{The \helmlab\ forward transform pipeline. Blue: linear operations; yellow: nonlinear corrections; green: structural guarantees (NC, rotation). All 72~parameters are jointly optimized.}
  \label{fig:pipeline}
\end{figure*}

\paragraph{Stage 1: Cone response (9 params).}
A learned $3\!\times\!3$ matrix $\mathbf{M}_1$ maps XYZ to
cone-response-like signals:
\begin{equation}
  \mathbf{c} = \mathbf{M}_1 \begin{bmatrix} X \\ Y \\ Z \end{bmatrix}
\end{equation}
Unlike fixed matrices (Hunt--Pointer--Est\'evez, Bradford),
$\mathbf{M}_1$ is freely optimized.

\paragraph{Stage 2: Power compression (3 params).}
Each channel undergoes signed power compression:
\begin{equation}
  c_i' = \mathrm{sign}(c_i)\,|c_i|^{\gamma_i}, \quad i \in \{0,1,2\}
\end{equation}
The exponents $\gamma_i$ generalize the cube-root of CIE~Lab
($\gamma = 1/3$). The optimized values are close to 0.4, which is
between the cube-root (0.33) and square-root (0.5) compressions
used in other spaces.

\paragraph{Stage 3: Lab projection (9 params).}
A second matrix $\mathbf{M}_2$ projects compressed signals to
opponent channels:
\begin{equation}
  \begin{bmatrix} L_\mathrm{raw} \\ a_\mathrm{raw} \\ b_\mathrm{raw} \end{bmatrix} = \mathbf{M}_2 \, \mathbf{c}'
\end{equation}

\paragraph{Stage 4: Hue correction (8 params).}
A 4-harmonic Fourier rotation corrects systematic hue distortions:
\begin{equation}
  \delta(h) = \sum_{k=1}^{4}\!\bigl[\alpha_k\cos(kh) + \beta_k\sin(kh)\bigr]
\end{equation}
where $h = \mathrm{atan2}(b, a)$. The chromatic vector $(a,b)$ is
rotated by $\delta(h)$, preserving chroma while redistributing
hue angles.

\paragraph{Stage 5: Helmholtz--Kohlrausch embedding (6 params).}
Saturated colors appear brighter than achromatic stimuli of equal
luminance~\cite{cie1976}. We embed this effect in the lightness
channel directly:
\begin{equation}
  L \mathrel{+}= w_\mathrm{HK} \cdot C^{p_\mathrm{HK}} \cdot \bigl[1 + f_\mathrm{HK}(h)\bigr]
  \label{eq:hk}
\end{equation}
where $C = \sqrt{a^2+b^2}$ and $f_\mathrm{HK}(h)$ is a 2-harmonic
hue modulation. The optimized weight is $w_\mathrm{HK} = 0.389$ and
the power is $p_\mathrm{HK} = 0.849$, making lightness explicitly
dependent on chroma. Most existing spaces treat lightness and chroma
as independent.

\paragraph{Stage 6: Lightness refinement (8 params).}
A cubic polynomial (3~params) with hue-dependent additive term
(2~params), followed by dark-region exponential compression (3~params):
\begin{align}
  L_1 &= p_1 L^3 + p_2 L^2 + p_3 L + t \cdot f_\mathrm{Lh}(h) \\
  L_2 &= L_1 \cdot \exp\!\bigl[\lambda_d(h) \cdot L_1(1-L_1)^2\bigr]
\end{align}
where $t = L(1-L)$ and $f_\mathrm{Lh}(h)$ is a 1-harmonic Fourier
correction targeting the hue--lightness interaction sign flip (dark
cyans vs mid cyans). The dark compression coefficient $\lambda_d$
includes hue modulation to handle the known difference in dark-blue
vs dark-yellow perception.

\paragraph{Stages 7--8: Chroma processing (18 params).}
Chroma processing proceeds in four interleaved sub-stages:
(a)~hue-dependent scaling (4~harmonics, 8~params):
\begin{equation}
  (a,b) \mathrel{*}= \exp\!\Bigl[\sum_{k=1}^{4}\!\bigl[\alpha'_k\cos(kh) + \beta'_k\sin(kh)\bigr]\Bigr]
\end{equation}
(b)~nonlinear chroma power (4~params): $C \to C^{1+\varepsilon(h)}$
where $\varepsilon(h)$ is a 2-harmonic Fourier series, separating
high-chroma from low-chroma discrimination;
(c)~L-dependent scaling (2~params):
$(a,b) \mathrel{*}= \exp[l_1(L-0.5) + l_2(L-0.5)^2]$; and
(d)~hue$\times$lightness interaction (4~params, 2-harmonic Fourier
modulated by $L-0.5$). The interleaving order matters: the nonlinear
power is applied between hue-dependent and L-dependent scaling to
allow each sub-stage to correct for the others' distortions.

\paragraph{Stage 9: Hue-dependent lightness (4 params).}
A final $L \mathrel{*}= \exp[g(h)]$ captures residual hue--lightness
interactions (\eg\ blue appears darker, yellow lighter at equal
luminance).

\paragraph{Stage 10: Neutral correction.}
With independent $\gamma_i$ per channel, the D65 achromatic axis
(stimuli proportional to $[X_{D65}, Y_{D65}, Z_{D65}]$) generally
does not map to $a\!=\!b\!=\!0$ after
the enrichment stages. We correct this by running 256 gray levels
($Y \in [0.001, 2.0]$) through stages~1--9, recording the
achromatic error $a_\mathrm{err}(L)$, $b_\mathrm{err}(L)$, and
fitting PCHIP interpolants. At runtime:
\begin{equation}
  a \leftarrow a - a_\mathrm{err}(L), \quad b \leftarrow b - b_\mathrm{err}(L)
  \label{eq:nc}
\end{equation}
Because the correction is applied \emph{after} all nonlinear stages,
it accounts for every distortion in the pipeline. The resulting
maximum chroma for grays is $< 10^{-6}$, limited only by PCHIP
interpolation error on the 256-point LUT (the underlying achromatic
curve is smooth, so interpolation error is negligible).

\paragraph{Stage 11: Rigid rotation ($\varphi$).}
A uniform rotation of the $(a,b)$ plane by angle $\varphi$:
\begin{equation}
  \begin{bmatrix} a' \\ b' \end{bmatrix} = \begin{bmatrix} \cos\varphi & -\sin\varphi \\ \sin\varphi & \cos\varphi \end{bmatrix} \begin{bmatrix} a \\ b \end{bmatrix}
  \label{eq:rotation}
\end{equation}
This is an isometry: it preserves $\Delta a^2 + \Delta b^2$ for any
pair, and therefore preserves the distance metric exactly
(\Cref{sec:rotation-proof}). The rotation angle
$\varphi = -28.2$\textdegree\ is chosen to minimize the maximum
hue-angle error across sRGB primaries and secondaries.

\subsection{Rotation Invariance}
\label{sec:rotation-proof}

The distance metric (\Cref{sec:distance}) depends on
$\Delta L$, $\Delta a^2 + \Delta b^2$, $\bar{L}$, and
$\bar{C} = \sqrt{\bar{a}^2 + \bar{b}^2}$. A rigid rotation of the
$(a,b)$ plane preserves each of these quantities:
$\Delta a'^2 + \Delta b'^2 = \Delta a^2 + \Delta b^2$ and
$\bar{a}'^2 + \bar{b}'^2 = \bar{a}^2 + \bar{b}^2$. Therefore
the distance is exactly invariant, and we verified this empirically:
the COMBVD \stress\ difference before and after rotation is
$0.0000000000$.

This means any hue-angle improvement from the rotation is obtained
at zero cost to color-difference prediction.

\subsection{Surround Parameter}
\label{sec:surround}

\helmlab\ includes provisions for a surround luminance parameter
$S \in [0, 1]$ (0=dark, 0.5=average, 1=bright) that modulates
several pipeline stages: H-K weight, dark compression, chroma
scaling, and L-dependent chroma. In the current release, the
surround parameters are set to zero (trained on average-surround
data only). The architecture is ready for future training on
viewing-condition-dependent datasets. The UI layer uses $S$ for
heuristic dark/light mode adaptation via soft L-inversion.

\subsection{Inverse Transform}

Each stage has an explicit inverse. Matrix stages invert via
$\mathbf{M}_1^{-1}$, $\mathbf{M}_2^{-1}$; power compression
via $c_i = \mathrm{sign}(c_i')|c_i'|^{1/\gamma_i}$ (closed form).
Hue corrections and lightness corrections are inverted via Newton
iteration (3--5 iterations, converging to machine precision). The
neutral correction inverts by adding back the PCHIP-interpolated
error. Round-trip accuracy across the full sRGB gamut is
$< 10^{-14}$.


% ══════════════════════════════════════════════════════════════════
\section{Distance Metric}
\label{sec:distance}

Given two colors with coordinates $(L_1, a_1, b_1)$ and
$(L_2, a_2, b_2)$, we compute:
\begin{align}
  S_L &= 1 + s_L(\bar{L} - 0.5)^2 \label{eq:sl} \\
  S_C &= 1 + s_C \bar{C} \label{eq:sc}
\end{align}
where $\bar{L}$, $\bar{C}$ are pair averages. The raw distance is:
\begin{equation}
  d = \left[\left(\frac{\Delta L}{S_L}\right)^{\!2} + w_C \frac{\Delta a^2 + \Delta b^2}{S_C^2}\right]^{p/2}
  \label{eq:dist_raw}
\end{equation}
followed by monotonic compression and post-power:
\begin{equation}
  \Delta E = \left[\frac{d}{1 + c \cdot d}\right]^q
  \label{eq:dist_final}
\end{equation}

The pair-dependent weights $S_L$ and $S_C$ are inspired by
CIEDE2000 but learned from data (optimized values: $s_L = 0.001$,
$s_C = 0.022$). The Minkowski exponent $p = 0.804$ and
chroma weight $w_C = 1.046$ slightly favor chroma differences over
lightness. The compression $c = 1.590$ and post-power $q = 1.1$
reduce systematic bias for large color differences.

The distance metric contributes 7~parameters. Total system:
9+3+9+8+6+8+18+4 = 65~space + 7~metric = 72~parameters.


% ══════════════════════════════════════════════════════════════════
\section{Optimization}
\label{sec:optimization}

All 72 parameters are jointly optimized by minimizing:
\begin{equation}
  \mathcal{L} = \mathrm{STRESS}_\combvd + 0.05 \cdot \mathrm{STRESS}_\mathrm{He} + \mathcal{L}_\mathrm{blue}
  \label{eq:loss}
\end{equation}
using L-BFGS-B~\cite{byrd1995} with box constraints. We perform
10~random restarts (each 5{,}000 iterations) and keep the best. The
He~\etal\ term ($\lambda = 0.05$) acts as a mild regularizer against
overfitting to \combvd.

The blue-band penalty is:
\begin{equation}
  \mathcal{L}_\mathrm{blue} = \lambda_b(\mathrm{STRESS}_\mathrm{RIT} + \mathrm{STRESS}_\mathrm{LEEDS} + 0.5\,\mathrm{STRESS}_\mathrm{FullBlue})
  \label{eq:loss_blue}
\end{equation}
where $\lambda_b = 0.1$ and the subscripts denote \combvd\
sub-datasets with high blue-band content. Additional constraints
enforce round-trip accuracy ($\lambda_\mathrm{RT} = 100$),
achromatic integrity ($\lambda_\mathrm{ach} = 500$), and Munsell
uniformity ($0.1 \cdot \max(\mathrm{CV} - 20, 0)$). The blue-band
penalty was introduced in v2 of this paper to address gradient
non-uniformity in the blue--cyan region (\Cref{sec:blue-band}).

\paragraph{Training data.}
\combvd\ comprises 3{,}813 color pairs from six psychophysical
experiments: BFD-P(D65) (2{,}028), BFD-P(M) (548), BFD-P(C) (200),
Witt (418), RIT-DuPont (312), Leeds (307). All pairs have visual-difference
values on a common scale. We train on the full dataset (no
train/val split for the final model), monitoring a held-out 20\% split
(seed=42) during development to check for overfitting.

\paragraph{Cross-validation data.}
He~\etal\ 2022 (82~pairs, 10\textdegree\ observer) enters the loss as
a regularizer ($\lambda\!=\!0.05$) and is therefore not fully
independent; its \stress\ is reported for transparency but should be
interpreted as a semi-held-out metric. MacAdam~1974 (128~pairs,
10\textdegree) is held out entirely and never seen during
optimization.

\paragraph{Convergence.}
The optimizer typically converges within 3{,}000--5{,}000 function
evaluations per restart. Five-fold cross-validation (with
re-optimization per fold) yields a mean validation gap of $+0.98$
\stress\ points (std.\ $0.78$), indicating mild but acceptable
overfitting for a 72-parameter model.


% ══════════════════════════════════════════════════════════════════
\section{Evaluation}
\label{sec:eval}

\subsection{Color-Difference Prediction}

\Cref{fig:stress} and \Cref{tab:stress} show \stress\ on \combvd\ for
\helmlab\ and nine baseline methods. \helmlab\ achieves 23.30, compared to 29.18 for CIEDE2000
($-$20.2\%). A 10{,}000-iteration paired bootstrap gives
\helmlab\ 95\% CI $[22.50, 23.93]$ and CIEDE2000 95\% CI
$[27.64, 30.84]$. The intervals do not overlap ($p < 10^{-4}$).

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig1_stress.pdf}
  \caption{\stress\ on COMBVD (3{,}813 pairs). Lower is better.}
  \label{fig:stress}
\end{figure}

\begin{table}[t]
  \centering
  \caption{\stress\ on \combvd\ (3{,}813 pairs). Lower is better.}
  \label{tab:stress}
  \begin{tabular}{lrr}
    \toprule
    Method & \stress & vs CIEDE2000 \\
    \midrule
    \textbf{\helmlab} & \textbf{23.30} & \textbf{$-$20.2\%} \\
    CIEDE2000 & 29.18 & --- \\
    $\Delta E^*_{ab}$ (CIE76) & 42.80 & +46.7\% \\
    CIE94 & 33.59 & +15.1\% \\
    $\Delta E$ CMC & 34.04 & +16.6\% \\
    CAM16-UCS (Euclid.) & 33.90 & +16.2\% \\
    IPT (Euclid.) & 41.21 & +41.3\% \\
    $J_z a_z b_z$ (Euclid.) & 41.90 & +43.6\% \\
    Oklab (Euclid.) & 47.46 & +62.7\% \\
    sRGB (Euclid.) & 67.82 & +132.5\% \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig2_scatter.pdf}
  \caption{Predicted vs observed color differences on COMBVD.}
  \label{fig:scatter}
\end{figure}

The baselines in \Cref{tab:stress} use Euclidean distance in
each space's native Cartesian coordinates, without parametric weighting
or compression. Oklab's high \stress\ (47.46) is expected because
it was optimized for hue uniformity~\cite{ottosson2020}, not
color-difference prediction on \combvd. The \helmlab\ distance metric
includes pair-dependent weighting ($S_L$, $S_C$), a Minkowski exponent,
and monotonic compression, so the comparison is not on equal terms.
With Euclidean distance only, \helmlab\ gets \stress\ 27.6,
which already improves on CIEDE2000 (29.18) by 5.4\%. The full
distance metric then reduces \stress\ by a further 4.3~points.
Relative to CIEDE2000, the space transform and the distance metric
contribute roughly equally to the total improvement.

\subsection{Cross-Validation}

On He~\etal\ 2022 (regularizer, 82~pairs), \helmlab\ achieves
\stress\ 30.3 vs CIEDE2000's 32.6. On MacAdam~1974 (held out
entirely, 128~pairs), \helmlab\ achieves 20.3 vs CAM16-UCS's 18.7
(best). \Cref{fig:crossval} visualizes these results. \helmlab\ outperforms CIEDE2000 on He~\etal\ but
trails CAM16-UCS on MacAdam. This is consistent with what one
would expect from a model trained primarily on \combvd: it
generalizes reasonably but does not dominate on all evaluation sets.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig5_crossval.pdf}
  \caption{Cross-dataset validation on held-out data.}
  \label{fig:crossval}
\end{figure}

\subsection{Generation Properties: The Measurement--Generation Tradeoff}

A color space optimized for distance prediction does not necessarily
produce usable coordinates for color generation. During development,
the measurement-optimal configuration mapped neutral grays to chroma
$\bar{C} \approx 0.34$. The optimizer had deformed the achromatic
axis to reduce \stress, which made gray gradients unusable because
they showed visible color artifacts. Adding a penalty to constrain
grays to $C = 0$ did not help: the optimizer either stayed in the
measurement-optimal basin or escaped to a different basin with much
worse \stress\ (26--31). There was no smooth transition between the
two.

The neutral correction (\Cref{sec:space}, Stage~10) addresses this
problem. It subtracts the achromatic error \emph{after} all
enrichment stages, so the optimizer can find its measurement-optimal
parameters without constraint. The achromatic axis is then corrected
separately. The \stress\ increase from the neutral correction alone is $+0.04$
(from a measurement-only baseline of 23.18 to 23.22); the subsequent
blue-band refit (\Cref{sec:blue-band}) adds another $+0.08$,
yielding the final 23.30. Achromatic chroma drops from 0.34 to
$<10^{-6}$.

\Cref{fig:neutral} shows neutral ramp uniformity and achromatic chroma
leakage. \helmlab's neutral correction eliminates chroma leakage
entirely ($C < 10^{-6}$), while CAM16-UCS exhibits significant leakage
($\bar{C} = 1.42$).
\Cref{tab:gen} summarizes all generation properties.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig3_neutral.pdf}
  \caption{Left: neutral ramp step uniformity. Right: achromatic chroma
  leakage. \helmlab's NC guarantees $C < 10^{-6}$ for all grays.}
  \label{fig:neutral}
\end{figure}

\begin{table}[t]
  \centering
  \caption{Generation quality metrics.}
  \label{tab:gen}
  \begin{tabular}{lr}
    \toprule
    Property & Value \\
    \midrule
    Achromatic max $C$ & $<10^{-6}$ \\
    Hue RMS (6 primaries) & 18.1\textdegree \\
    Hue max error & 23.9\textdegree \\
    Round-trip $\|\cdot\|_\infty$ & $<10^{-14}$ \\
    Munsell CV & 20.0\% \\
    Jacobian det (min) & 0.10 \\
    Jacobian cond (median) & 7.7 \\
    \midrule
    5-fold CV gap (mean) & +0.98 \\
    He \stress & 30.33 \\
    MacAdam \stress & 20.33 \\
    \bottomrule
  \end{tabular}
\end{table}

The achromatic guarantee means gray gradients and lightness
ramps interpolate without color artifacts, which is necessary
for design-token generation and palette export. The hue RMS of
18.1\textdegree\ means sRGB primaries land within about
24\textdegree\ of their expected positions. This is acceptable
for palette generation but not perfect. The rigid rotation (\Cref{sec:space},
Stage~11) provides this hue improvement without affecting the
distance metric because the metric is invariant under rotation.
The Jacobian was evaluated on a
$64\!\times\!64\!\times\!64$ sRGB grid via finite differences;
the minimum determinant (0.10) is positive throughout, so the
map is locally invertible and orientation-preserving across the
entire sRGB gamut.

\subsection{Ablation Study}

\Cref{tab:ablation} isolates the contribution of key components
using \emph{frozen} ablation: each row disables one component while
keeping all other parameters at their optimized values. This measures
the marginal contribution of each component given the current
parameter setting, not its isolated effect.

\begin{table}[t]
  \centering
  \caption{Ablation: effect of removing components (frozen, not re-optimized).}
  \label{tab:ablation}
  \begin{tabular}{lrr}
    \toprule
    Configuration & \stress & $\Delta$ \\
    \midrule
    Full \helmlab\ & 23.30 & --- \\
    Euclidean distance only & 27.60 & +4.3 \\
    No H-K embedding ($w_\mathrm{HK}=0$) & 27.30 & +4.0 \\
    No hue correction & 38.81 & +15.5 \\
    No dark L compression & 23.49 & +0.2 \\
    \midrule
    Rotation ($-$28.2\textdegree) & 23.30 & 0.00 \\
    No rotation & 23.30 & 0.00 \\
    \bottomrule
  \end{tabular}
\end{table}

With Euclidean distance, the space alone gets \stress\ 27.6, which
already improves on CIEDE2000 (29.18) by 5.4\%. The full metric
brings it down to 23.30, recovering a further 4.3~points. Compared
to other Euclidean baselines (CAM16-UCS = 33.9), the space alone
gives a substantial improvement. Among individual stages, hue
correction is the most critical ($\Delta = +15.5$), followed by
the H-K embedding ($\Delta = +4.0$). These large frozen-ablation
deltas reflect the co-optimization of all parameters: disabling
one stage disrupts the balance of the entire pipeline. The rotation
row confirms that the metric is exactly invariant under rotation.

\subsection{Sub-Dataset Performance}

\Cref{tab:subdataset} breaks down \stress\ by \combvd\ sub-dataset.
\helmlab\ outperforms CIEDE2000 on 3 of 6 sub-datasets
(BFD-P(D65), BFD-P(M), Witt). On the blue-heavy sub-datasets
(LEEDS, RIT-DuPont), the blue-band refit reduces the gap
substantially compared to an earlier version without the penalty:
RIT-DuPont improves from 23.49 to 20.41, nearly matching
CIEDE2000's 19.51. BFD-P(C) is the only sub-dataset measured
under illuminant~C (rather than D65) and with a 2\textdegree\
observer, which may explain the remaining deficit.

\begin{table}[t]
  \centering
  \caption{\stress\ on individual \combvd\ sub-datasets.}
  \label{tab:subdataset}
  \begin{tabular}{lrrr}
    \toprule
    Sub-dataset & $N$ & \helmlab & CIEDE2000 \\
    \midrule
    BFD-P(D65)  & 2{,}028 & \textbf{23.11} & 24.14 \\
    BFD-P(M)    & 548     & \textbf{22.22} & 35.09 \\
    BFD-P(C)    & 200     & 30.15          & \textbf{30.05} \\
    Witt        & 418     & \textbf{28.88} & 30.37 \\
    Leeds       & 307     & 20.92          & \textbf{19.46} \\
    RIT-DuPont  & 312     & 20.41          & \textbf{19.51} \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Blue-Band Analysis}
\label{sec:blue-band}

During development, we observed that an initial version of
\helmlab\ (without the blue-band penalty) exhibited severe gradient
non-uniformity in the blue--cyan region. A red-to-blue gradient
interpolated in \helmlab\ Lab coordinates showed a $51.4\times$
max/min step-size ratio when measured with CIEDE2000, compared to
$1.3\times$ for the same gradient in CIE~Lab. Frozen ablation
traced the problem to the 4-harmonic hue correction, whose Fourier
coefficients overshoot in the blue--magenta band
($h \approx 240$--$340$\textdegree). Disabling hue correction
reduced the gradient ratio from $51.4\times$ to $4.9\times$ but
increased \combvd\ \stress\ by $+15.5$~points, making this an
unacceptable trade-off.

The solution was to add explicit sub-dataset penalties to the loss
function (\Cref{eq:loss_blue}) and re-optimize all 72~parameters.
With $\lambda_b = 0.1$, the red-to-blue gradient ratio drops from
$51.4\times$ to $5.8\times$ ($8.9\times$ improvement), while
\combvd\ \stress\ increases by only $+0.08$ (from 23.22 to 23.30).
\stress\ improves on 5 of 6 sub-datasets (\Cref{tab:subdataset}),
with the largest gain on RIT-DuPont ($-3.08$~points). The only
regression is BFD-P(D65) ($+0.49$), the largest sub-dataset, which
accounts for the slight overall \stress\ increase.


% ══════════════════════════════════════════════════════════════════
\section{Practical Adoption}
\label{sec:adoption}

\Cref{fig:gamut} shows the sRGB gamut boundary in \helmlab\ coordinates
at three lightness levels. The boundaries are smooth and well-behaved.
\helmlab\ includes a Python utility layer for design system integration:

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig6_gamut.pdf}
  \caption{sRGB gamut in \helmlab\ $a$--$b$ plane at three L levels.
  Points colored by their sRGB values.}
  \label{fig:gamut}
\end{figure}

\paragraph{Gamut mapping.}
Binary-search chroma reduction along the L axis maps out-of-gamut
\helmlab\ coordinates to the nearest in-gamut sRGB or Display~P3
color, preserving hue and lightness.

\paragraph{Contrast utilities.}
\texttt{ensure\_contrast(fg, bg, min\_ratio)} adjusts foreground
lightness via binary search on the \helmlab\ L axis to meet
WCAG~2.x contrast requirements (4.5:1 for AA, 7:1 for AAA).
Hue and chroma are preserved.

\paragraph{Palette generation.}
Lightness ramps and hue rings at fixed chroma produce
perceptually-spaced palettes. Semantic scales (Tailwind-style
50--950) are generated by interpolating L between 0.97 and 0.10
with chroma/hue preserved.

\paragraph{Dark/light mode.}
When surround parameters are trained, the surround $S$ directly
modulates the transform. Currently, a fallback soft L-inversion
maps colors between light ($S=0.7$) and dark ($S=0.2$) modes
with contrast enforcement.

\paragraph{Token export.}
A single \texttt{TokenExporter} produces CSS custom properties
(\texttt{oklch()}, \texttt{color(display-p3 ...)}), Android XML
(using HCT tone/chroma/hue semantics for Material Design
compatibility), iOS Swift (Display~P3), Tailwind config, and
raw JSON.


% ══════════════════════════════════════════════════════════════════
\section{Limitations and Future Work}
\label{sec:limitations}

There are several limitations worth noting:

\begin{enumerate}
  \item \textbf{Training data.} \combvd\ predominantly uses the
    CIE~1964 10\textdegree\ standard observer ($\sim$95\% of pairs).
    Only BFD-P(C) (200~pairs, 5\%) uses the 2\textdegree\ observer.
    Gao~\etal\ 2023~\cite{gao2023} showed that the difference between
    2\textdegree\ and 10\textdegree\ observers is not statistically
    significant for \stress\ evaluation, and MacAdam~1974 (measured
    with the 2\textdegree\ observer) cross-validation confirms
    reasonable generalization.

  \item \textbf{Model complexity.} 72 parameters is considerably more
    than Oklab ($\sim$15 designed parameters). Each stage has a
    perceptual motivation, but the overall model is harder to
    interpret than simpler spaces.

  \item \textbf{Hue alignment.} RMS hue error of 18.1\textdegree\ is
    a compromise. Some hue regions (cyan) have errors up to
    23.9\textdegree. Further harmonic terms could improve this at the
    risk of overfitting.

  \item \textbf{Surround dependency.} The surround parameter $S$ is
    architecturally present but not yet trained on viewing-condition
    data. Current dark/light adaptation uses heuristics.

  \item \textbf{Sub-dataset variation.} \helmlab\ underperforms
    CIEDE2000 on 3 of 6 \combvd\ sub-datasets
    (\Cref{tab:subdataset}). The blue-band refit improves 5 of 6
    sub-datasets compared to the previous version; only BFD-P(C),
    measured under illuminant~C with a 2\textdegree\ observer,
    remains notably worse (30.15 vs 30.05).

  \item \textbf{Not general-purpose.} \helmlab\ is optimized for UI
    workflows (sRGB/P3 gamut, screen viewing). It is not designed for
    print, photography, or spectral applications.

  \item \textbf{Blue-band trade-off.} The blue-band refit
    (\Cref{sec:blue-band}) reduces gradient non-uniformity by
    $8.9\times$ at a cost of $+0.08$ \stress\ on \combvd. The
    Munsell CV increases from 17.6\% to 20.0\%, hue RMS from
    16.1\textdegree\ to 18.1\textdegree, and the He~\etal\
    cross-validation \stress\ increases from 29.0 to 30.3.

  \item \textbf{Statistical reporting.} Bootstrap confidence intervals
    confirm the aggregate improvement over CIEDE2000 ($p < 10^{-4}$),
    but per-sub-dataset significance and parameter uncertainty across
    restarts remain unquantified.
\end{enumerate}

The open-source release includes a second, generation-optimized space
(\textsc{GenSpace}) with shared $\gamma\!=\!\tfrac{1}{3}$ and no
enrichment stages, which trades distance accuracy for faster, invertible
gradient and palette generation.
For future work, we plan to train the surround parameter on paired
light/dark viewing data and to expand the training set with targeted
human feedback using the bidirectional optimization framework that is
already in the codebase.


% ══════════════════════════════════════════════════════════════════
\section{Conclusion}
\label{sec:conclusion}

We have presented \helmlab, a 72-parameter analytical color space
that achieves \stress\ 23.30 on \combvd, which is 20.2\% lower than
CIEDE2000. The achromatic axis is guaranteed to have $C < 10^{-6}$
via the neutral correction, and a rigid rotation brings hue error
down to 18.1\textdegree\ RMS without affecting the distance metric.
A blue-band refit with sub-dataset penalties reduces gradient
non-uniformity in the blue--cyan region by $8.9\times$ at a cost
of only $+0.08$ \stress\ points, while improving \stress\ on 5 of 6
sub-datasets. The neutral correction is the key element that makes
the space usable for both measurement and generation: it lets the
optimizer find its best parameters freely and then fixes the
achromatic axis.
Code, trained parameters, and utilities are available at
\url{https://github.com/Grkmyldz148/helmlab}. An interactive demo
is at \url{https://grkmyldz148.github.io/helmlab/demo.html} and
documentation at \url{https://grkmyldz148.github.io/helmlab/}.


% ══════════════════════════════════════════════════════════════════
\section*{Acknowledgments}

Thanks to the 71~anonymous observers who participated in the
bidirectional evaluation study during development.


% ══════════════════════════════════════════════════════════════════
\bibliographystyle{plain}
\begin{thebibliography}{99}

\bibitem{cie1976}
CIE, ``Colorimetry,'' CIE Publication 15, 1976.

\bibitem{luo2001}
M.~R. Luo, G.~Cui, and B.~Rigg, ``The development of the CIE 2000
colour-difference formula: CIEDE2000,'' \textit{Color Res.\ Appl.},
vol.~26, no.~5, pp.~340--350, 2001.

\bibitem{ottosson2020}
B.~Ottosson, ``A perceptual color space for image processing,''
\url{https://bottosson.github.io/posts/oklab/}, 2020.

\bibitem{li2017}
C.~Li, Z.~Li, Z.~Wang, \textit{et al.}, ``Comprehensive color
solutions: CAM16, CAT16, and S-decoupling,'' \textit{Color Res.\
Appl.}, vol.~42, no.~6, pp.~703--718, 2017.

\bibitem{ebner1998}
F.~Ebner and M.~D. Fairchild, ``Development and testing of a color
space (IPT) with improved hue uniformity,'' in \textit{Proc.\ IS\&T
CIC6}, 1998.

\bibitem{safdar2017}
M.~Safdar, G.~Cui, Y.~J. Kim, and M.~R. Luo, ``Perceptually uniform
color space for image signals including high dynamic range and wide
gamut,'' \textit{Opt.\ Express}, vol.~25, no.~13, 2017.

\bibitem{luo2006}
M.~R. Luo, G.~Cui, and C.~Li, ``Uniform colour spaces based on
CIECAM02 colour appearance model,'' \textit{Color Res.\ Appl.},
vol.~31, pp.~320--330, 2006.

\bibitem{he2022}
R.~He, G.~Cui, T.~Zhu, and M.~R. Luo, ``Colour difference
evaluation using large colour differences,'' in \textit{Proc.\ CIE},
2022.

\bibitem{macadam1974}
D.~L. MacAdam, ``Uniform color scales,'' \textit{J.\ Opt.\ Soc.\
Am.}, vol.~64, pp.~1691--1702, 1974.

\bibitem{byrd1995}
R.~H. Byrd, P.~Lu, J.~Nocedal, and C.~Zhu, ``A limited memory
algorithm for bound constrained optimization,'' \textit{SIAM J.\ Sci.\
Comput.}, vol.~16, pp.~1190--1208, 1995.

\bibitem{gao2023}
Y.~Gao, M.~R. Luo, M.~R. Pointer, and C.~Li, ``Evaluation of
color difference prediction with CIECAM16 using CIE 2- and 10-degree
observers,'' \textit{J.\ Imaging Sci.\ Technol.}, 2023.

\end{thebibliography}


% ══════════════════════════════════════════════════════════════════
\appendix
\section{Parameter Table}
\label{app:params}

\Cref{tab:params} lists all 72 optimized parameters grouped by
pipeline stage.

\begin{table*}[t]
  \centering
  \caption{Optimized \helmlab\ parameters (v20b, $\varphi = -28.2$\textdegree).}
  \label{tab:params}
  \footnotesize
  \begin{tabular}{llrl}
    \toprule
    Stage & Parameter & Value & Description \\
    \midrule
    \multirow{3}{*}{$\mathbf{M}_1$ (9)} & Row 0 & [0.734, 0.240, $-$0.158] & XYZ $\to$ cone-like \\
    & Row 1 & [$-$0.330, 1.235, $-$0.000] & \\
    & Row 2 & [0.086, 0.341, 0.832] & \\
    \midrule
    $\gamma$ (3) & $\gamma_0, \gamma_1, \gamma_2$ & 0.389, 0.416, 0.424 & Power compression \\
    \midrule
    \multirow{3}{*}{$\mathbf{M}_2$ (9)} & Row 0 & [$-$0.420, 0.485, 0.642] & Compressed $\to$ Lab \\
    & Row 1 & [1.933, $-$2.718, 0.764] & \\
    & Row 2 & [0.006, 1.628, $-$1.261] & \\
    \midrule
    Hue corr. (8) & $\alpha_{1..4}, \beta_{1..4}$ & see JSON & 4-harmonic Fourier \\
    \midrule
    H-K (6) & $w, p, m, s_1, c_2, s_2$ & 0.389, 0.849, $-$0.494, ... & Chroma $\to$ L \\
    \midrule
    Light. (8) & $p_1, p_2, p_3, Lh_{c1}, Lh_{s1}, \lambda_d, h_c, h_s$ & 0.222, $-$0.727, 0.695, ... & Cubic + Lh + dark \\
    \midrule
    Chroma (18) & 4-harm + power + $lc_1, lc_2$ + HLC & see JSON & Hue/power/L-dep \\
    \midrule
    Hue-L (4) & $g_{c1..2}, g_{s1..2}$ & see JSON & Hue $\to$ L \\
    \midrule
    \multirow{4}{*}{Distance (7)} & $s_L$ & $1.01 \times 10^{-3}$ & Pair-dep L weight \\
    & $s_C$ & 0.022 & Pair-dep C weight \\
    & $p$ & 0.804 & Minkowski exponent \\
    & $w_C$ & 1.046 & Chroma weight \\
    & $c$ & 1.590 & Compression \\
    & $q$ & 1.100 & Post-power \\
    & $\alpha$ & 0.000 & Linear term \\
    \bottomrule
  \end{tabular}
\end{table*}

Full parameter values are available in the accompanying
\texttt{metric\_params.json} file.

\end{document}
