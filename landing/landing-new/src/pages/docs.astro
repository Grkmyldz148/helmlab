---
import BaseLayout from "../layouts/BaseLayout.astro";
import { claims, ciede2000ImprovementPct } from "../data/claims";

const sections = [
  { id: "installation", label: "Installation" },
  { id: "quick-start", label: "Quick Start" },
  { id: "genspace", label: "GenSpace" },
  { id: "metricspace", label: "MetricSpace" },
  { id: "helmlab-class", label: "Helmlab Class" },
  { id: "colorjs", label: "Color.js" },
  { id: "ai-skills", label: "AI Skills" },
  { id: "token-export", label: "Token Export" },
  { id: "typescript", label: "TypeScript" },
  { id: "advanced", label: "Advanced" },
];
---

<BaseLayout
  title="Docs — Helmlab"
  description="API documentation for Helmlab color space. Python and JavaScript with identical APIs. GenSpace for generation, MetricSpace for measurement."
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-20">
    <div class="lg:grid lg:grid-cols-[220px_1fr] lg:gap-10">

      <!-- Sidebar TOC (desktop: sticky, mobile: dropdown) -->
      <aside class="mb-8 lg:mb-0">
        <!-- Mobile dropdown -->
        <div class="lg:hidden mb-6">
          <label for="docs-toc-toggle" class="flex items-center justify-between w-full rounded-lg border border-white/[0.06] bg-[#0c0c0f] px-4 py-3 text-sm font-medium text-[#a1a1aa] cursor-pointer">
            <span>On this page</span>
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
            </svg>
          </label>
          <input type="checkbox" id="docs-toc-toggle" class="peer hidden" />
          <nav class="hidden peer-checked:block mt-2 rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-3">
            <ul class="space-y-1">
              {sections.map((s) => (
                <li>
                  <a href={`#${s.id}`} class="block rounded-md px-3 py-2 text-sm text-[#a1a1aa] transition-colors hover:text-white hover:bg-white/[0.04]">
                    {s.label}
                  </a>
                </li>
              ))}
            </ul>
          </nav>
        </div>

        <!-- Desktop sticky sidebar -->
        <nav class="hidden lg:block sticky top-20">
          <div class="relative mb-4">
            <input
              id="docs-search"
              type="search"
              placeholder="Search docs… (⌘K)"
              autocomplete="off"
              class="w-full rounded-md border border-white/[0.08] bg-[#0c0c0f] px-3 py-1.5 text-sm text-[#fafafa] placeholder:text-[#52525b] focus:border-[#f97316]/40 focus:outline-none"
            />
            <div
              id="docs-search-results"
              class="hidden absolute z-30 mt-1 w-full max-h-80 overflow-auto rounded-md border border-white/[0.08] bg-[#0c0c0f] p-1 shadow-xl"
            ></div>
          </div>
          <p class="text-xs uppercase tracking-widest text-[#52525b] font-medium mb-4">On this page</p>
          <ul class="space-y-1">
            {sections.map((s) => (
              <li>
                <a href={`#${s.id}`} class="block rounded-md px-3 py-1.5 text-sm text-[#a1a1aa] transition-colors hover:text-white hover:bg-white/[0.04]">
                  {s.label}
                </a>
              </li>
            ))}
          </ul>
          <div class="mt-8 pt-6 border-t border-white/[0.06]">
            <a href="https://github.com/Grkmyldz148/helmlab/edit/main/landing/landing-new/src/pages/docs.astro" target="_blank" rel="noopener noreferrer" class="text-xs text-[#52525b] hover:text-[#a1a1aa] transition-colors">
              Edit on GitHub
            </a>
          </div>
        </nav>
      </aside>

      <!-- Main content -->
      <div class="max-w-4xl">
        <h1 class="text-3xl sm:text-4xl font-bold mb-3">Documentation</h1>
        <p class="text-[#a1a1aa] text-lg mb-12">
          Python and JavaScript with identical APIs. TypeScript types included.
        </p>

        <!-- Language tab switcher (CSS-only via radio inputs) -->
        <div class="docs-lang-switcher flex gap-1 mb-12 rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-1 w-fit">
          <input type="radio" name="docs-lang" id="lang-python" value="python" class="peer/py hidden" checked />
          <label for="lang-python" class="docs-lang-btn rounded-md px-4 py-2 text-sm font-medium cursor-pointer transition-colors text-[#52525b] peer-checked/py:bg-white/[0.06] peer-checked/py:text-[#fafafa]">
            Python
          </label>
          <input type="radio" name="docs-lang" id="lang-js" value="js" class="peer/js hidden" />
          <label for="lang-js" class="docs-lang-btn rounded-md px-4 py-2 text-sm font-medium cursor-pointer transition-colors text-[#52525b] peer-checked/js:bg-white/[0.06] peer-checked/js:text-[#fafafa]">
            JavaScript / TypeScript
          </label>
        </div>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- INSTALLATION -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="installation" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">Installation</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              Helmlab works in both Python and JavaScript. Pick whichever your team already uses. If you just want to try it out, the JS package is the easiest to get going with.
            </p>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              Zero dependencies in the JS package ({claims.bundleKB}KB gzipped). Python requires NumPy. Both packages ship the same default parameters and produce identical output.
            </p>
          </div>

          <!-- Python install -->
          <div class="docs-py mb-4">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Python</span>
<span class="text-[#34d399]">pip install helmlab</span></code></pre>
          </div>

          <!-- JS install -->
          <div class="docs-js mb-4">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># npm</span>
<span class="text-[#34d399]">npm install helmlab</span>

<span class="text-[#52525b]"># or yarn / pnpm</span>
<span class="text-[#34d399]">yarn add helmlab</span></code></pre>
          </div>

          <div class="for-dev">
            <p class="text-sm text-[#52525b] mt-4">
              PostCSS plugin also available: <code class="text-[#a1a1aa] bg-white/[0.04] px-1.5 py-0.5 rounded text-xs">npm install postcss-helmlab</code>
            </p>
            <p class="text-sm text-[#52525b] mt-2">
              Helmlab is also merged into Color.js master (3 spaces, pending next release) — see <a href="#colorjs" class="text-[#a1a1aa] hover:text-white underline-offset-2 hover:underline">Using with Color.js</a> below.
            </p>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- QUICK START -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="quick-start" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">Quick Start</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              The <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">Helmlab</code> class is the main entry point. It gives you everything: palettes, gradients, contrast checking, and color measurement -- all in one place.
            </p>
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4 mb-4">
              <p class="text-sm text-[#a1a1aa]">
                <strong class="text-[#fafafa]">What you get:</strong> Pass in any hex color and get back a full palette, smooth gradients, or a Tailwind-style 50-950 semantic scale. Every output is gamut-mapped to valid sRGB.
              </p>
            </div>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">Helmlab</code> is the high-level API wrapping both GenSpace (generation) and MetricSpace (measurement). It accepts hex strings, returns hex strings, and handles gamut mapping internally.
            </p>
          </div>

          <!-- Python quick start -->
          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab</span> <span class="text-[#c084fc]">import</span> Helmlab

hl = Helmlab()

<span class="text-[#52525b]"># Generate a 10-shade palette</span>
palette = hl.<span class="text-[#34d399]">palette</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, steps=<span class="text-[#f97316]">10</span>)

<span class="text-[#52525b]"># Smooth gradient from red to blue (16 steps)</span>
gradient = hl.<span class="text-[#34d399]">gradient</span>(<span class="text-[#fb923c]">"#ef4444"</span>, <span class="text-[#fb923c]">"#3b82f6"</span>, steps=<span class="text-[#f97316]">16</span>)

<span class="text-[#52525b]"># Color distance (Euclidean Lab — fast, uncompressed)</span>
dist = hl.<span class="text-[#34d399]">delta_e</span>(<span class="text-[#fb923c]">"#ff0000"</span>, <span class="text-[#fb923c]">"#00ff00"</span>)

<span class="text-[#52525b]"># Ensure WCAG AA contrast (adjusts color if needed)</span>
adjusted = hl.<span class="text-[#34d399]">ensure_contrast</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, <span class="text-[#fb923c]">"#ffffff"</span>, min_ratio=<span class="text-[#f97316]">4.5</span>)

<span class="text-[#52525b]"># Tailwind-style semantic scale</span>
scale = hl.<span class="text-[#34d399]">semantic_scale</span>(<span class="text-[#fb923c]">"#3b82f6"</span>)</code></pre>
          </div>

          <!-- JS quick start -->
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} Helmlab {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#c084fc]">const</span> hl = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Helmlab</span>();

<span class="text-[#52525b]">// Generate a 10-shade palette</span>
<span class="text-[#c084fc]">const</span> palette = hl.<span class="text-[#34d399]">palette</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#f97316]">10</span>);

<span class="text-[#52525b]">// Smooth gradient from red to blue (16 steps)</span>
<span class="text-[#c084fc]">const</span> gradient = hl.<span class="text-[#34d399]">gradient</span>(<span class="text-[#fb923c]">'#ef4444'</span>, <span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#f97316]">16</span>);

<span class="text-[#52525b]">// Color distance (Euclidean Lab — fast, uncompressed)</span>
<span class="text-[#c084fc]">const</span> dist = hl.<span class="text-[#34d399]">deltaE</span>(<span class="text-[#fb923c]">'#ff0000'</span>, <span class="text-[#fb923c]">'#00ff00'</span>);

<span class="text-[#52525b]">// Ensure WCAG AA contrast (adjusts color if needed)</span>
<span class="text-[#c084fc]">const</span> adjusted = hl.<span class="text-[#34d399]">ensureContrast</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#f97316]">4.5</span>);

<span class="text-[#52525b]">// Tailwind-style semantic scale</span>
<span class="text-[#c084fc]">const</span> scale = hl.<span class="text-[#34d399]">semanticScale</span>(<span class="text-[#fb923c]">'#3b82f6'</span>);</code></pre>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- GENSPACE -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="genspace" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">GenSpace</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              GenSpace is the color space optimized for <strong class="text-[#fafafa]">creating</strong> colors -- gradients, palettes, and gamut mapping. It produces smooth, visually pleasing gradients that keep slightly more saturation through the blue→white midpoint than OKLab (both stay blue; the gap is small but consistent).
            </p>
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4 mb-4">
              <p class="text-sm text-[#a1a1aa]">
                <strong class="text-[#fafafa]">When to use:</strong> Generating color palettes, creating gradients, building design tokens, gamut mapping for display.
              </p>
            </div>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              GenSpace uses a depressed cubic transfer function (<code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">y&sup3; + &alpha;y = x</code>) with L-gated hue enrichment. Pipeline: <code class="text-[#a1a1aa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">XYZ &rarr; M1 &rarr; depcubic &rarr; M2 &rarr; PW_L &rarr; enrichment &rarr; C^cp &rarr; NC &rarr; Lab</code>. All transforms have exact analytical inverses.
            </p>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-8">Forward / Inverse Transform</h3>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab</span> <span class="text-[#c084fc]">import</span> Helmlab, GenSpace

hl = Helmlab()
gen = GenSpace()

<span class="text-[#52525b]"># sRGB [0,1] &rarr; GenSpace Lab [L, a, b]  (via the Helmlab wrapper)</span>
lab = hl.<span class="text-[#34d399]">gen_from_srgb</span>([<span class="text-[#f97316]">0.231</span>, <span class="text-[#f97316]">0.510</span>, <span class="text-[#f97316]">0.965</span>])

<span class="text-[#52525b]"># GenSpace Lab &rarr; sRGB [0,1]</span>
rgb = hl.<span class="text-[#34d399]">gen_to_srgb</span>(lab)

<span class="text-[#52525b]"># XYZ &rarr; Lab and back (GenSpace directly)</span>
lab = gen.<span class="text-[#34d399]">from_XYZ</span>(xyz)
xyz = gen.<span class="text-[#34d399]">to_XYZ</span>(lab)</code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} Helmlab, GenSpace {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#c084fc]">const</span> hl = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Helmlab</span>();
<span class="text-[#c084fc]">const</span> gen = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">GenSpace</span>();

<span class="text-[#52525b]">// sRGB [0,1] &rarr; GenSpace Lab [L, a, b]  (via the Helmlab wrapper)</span>
<span class="text-[#c084fc]">const</span> lab = hl.<span class="text-[#34d399]">genFromSrgb</span>([<span class="text-[#f97316]">0.231</span>, <span class="text-[#f97316]">0.510</span>, <span class="text-[#f97316]">0.965</span>]);

<span class="text-[#52525b]">// GenSpace Lab &rarr; sRGB [0,1]</span>
<span class="text-[#c084fc]">const</span> rgb = hl.<span class="text-[#34d399]">genToSrgb</span>(lab);

<span class="text-[#52525b]">// XYZ &rarr; Lab and back (GenSpace directly)</span>
<span class="text-[#c084fc]">const</span> lab2 = gen.<span class="text-[#34d399]">fromXYZ</span>(xyz);
<span class="text-[#c084fc]">const</span> xyz2 = gen.<span class="text-[#34d399]">toXYZ</span>(lab2);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">Gradient Generation</h3>

          <div class="for-designer mb-4">
            <p class="text-[#a1a1aa] mb-4">
              Helmlab gradients use arc-length reparameterization to ensure each step looks equally spaced to human eyes. This means no bunching near the endpoints and no sudden jumps in the middle.
            </p>
          </div>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab</span> <span class="text-[#c084fc]">import</span> Helmlab

hl = Helmlab()

<span class="text-[#52525b]"># 16-step gradient with arc-length reparameterization</span>
grad = hl.<span class="text-[#34d399]">gradient</span>(<span class="text-[#fb923c]">"#ef4444"</span>, <span class="text-[#fb923c]">"#3b82f6"</span>, steps=<span class="text-[#f97316]">16</span>)
<span class="text-[#52525b]"># Returns: ['#ef4444', '#e64f55', '#dc5864', ..., '#3b82f6']</span></code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} Helmlab {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#c084fc]">const</span> hl = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Helmlab</span>();

<span class="text-[#52525b]">// 16-step gradient with arc-length reparameterization</span>
<span class="text-[#c084fc]">const</span> grad = hl.<span class="text-[#34d399]">gradient</span>(<span class="text-[#fb923c]">'#ef4444'</span>, <span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#f97316]">16</span>);
<span class="text-[#52525b]">// Returns: ['#ef4444', '#e64f55', '#dc5864', ..., '#3b82f6']</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">Palette Generation</h3>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Generate a 10-shade palette (light to dark)</span>
palette = hl.<span class="text-[#34d399]">palette</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, steps=<span class="text-[#f97316]">10</span>)
<span class="text-[#52525b]"># Returns: ['#fafcff', '#ccdeff', ..., '#3e86fa', ..., '#000046']</span></code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Generate a 10-shade palette (light to dark)</span>
<span class="text-[#c084fc]">const</span> palette = hl.<span class="text-[#34d399]">palette</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#f97316]">10</span>);
<span class="text-[#52525b]">// Returns: ['#fafcff', '#ccdeff', ..., '#3e86fa', ..., '#000046']</span></code></pre>
          </div>

          <div class="for-dev mt-6">
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4">
              <p class="text-sm text-[#a1a1aa]">
                <strong class="text-[#fafafa]">Key properties:</strong> 360/360/360 valid cusps across sRGB, Display P3, and Rec.2020. Zero monotonicity violations in sRGB/P3 (1 in Rec.2020). Blue-to-white gradient midpoint (G/R ≈ 1.5) stays a touch more saturated than OKLab (≈ 1.4) — both blue. Achromatic precision: C* ≈ 10<sup>-15</sup>.
              </p>
            </div>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- METRICSPACE -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="metricspace" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">MetricSpace</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              MetricSpace is optimized for <strong class="text-[#fafafa]">measuring</strong> how different two colors look. It answers the question "how far apart do these colors appear to a human?" more accurately than any existing formula, including the industry-standard CIEDE2000.
            </p>
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4 mb-4">
              <p class="text-sm text-[#a1a1aa]">
                <strong class="text-[#fafafa]">When to use:</strong> Comparing colors, finding closest matches, evaluating palette consistency, quality control for design tokens.
              </p>
            </div>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              {claims.metricParams}-parameter enriched pipeline jointly optimized against COMBVD ({claims.datasets.combvdPairs.toLocaleString()} pairs, 64,000+ human judgments). STRESS {claims.stress.metricSpace.combvd} -- {ciede2000ImprovementPct()}% better than CIEDE2000's {claims.stress.ciede2000.combvd}. Pipeline: <code class="text-[#a1a1aa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">XYZ &rarr; M1 &rarr; &gamma; &rarr; M2 &rarr; Hue &rarr; H-K &rarr; L &rarr; C &rarr; HL &rarr; NC &rarr; &phi; &rarr; Lab</code>.
            </p>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-8">Color Difference</h3>

          <p class="text-[#a1a1aa] mb-4 text-sm">
            The recommended entry point is
            <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">difference()</code>
            — it returns the trained perceptual distance directly from hex (the
            full Minkowski + compression metric optimized against COMBVD,
            STRESS {claims.stress.metricSpace.combvd}, {ciede2000ImprovementPct()}% better than CIEDE2000). Two lower-level metrics
            are also exposed:
            <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">euclidean_distance()</code>
            (a clearer alias of
            <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">delta_e()</code>)
            returns uncompressed Euclidean Lab — ΔE76-style, fast, for quick UI
            comparisons — and
            <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">perceptual_distance()</code>
            is the same trained metric on Lab inputs.
          </p>
          <p class="text-[#a1a1aa] mb-4 text-sm">
            <span class="text-[#fafafa]">New (experimental):</span>
            <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">difference_with_confidence()</code>
            returns the difference <em>plus how much observers will disagree about
            it</em> — including <code class="text-[#fafafa] bg-white/[0.04] px-1 rounded">p_noticeable</code>,
            the chance a random observer rates the pair at least moderately different
            (small / low-chroma differences come back unreliable). v2: ordered-probit
            refit of our 47-pair × 71-observer set — the categorical ratings are
            modelled as thresholds on a latent axis, fixing v1's inverted
            disagreement target.
            Available in both Python (<code class="text-[#fafafa] bg-white/[0.04] px-1 rounded">difference_with_confidence</code>)
            and JS (<code class="text-[#fafafa] bg-white/[0.04] px-1 rounded">differenceWithConfidence</code>) — identical output.
          </p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab</span> <span class="text-[#c084fc]">import</span> Helmlab

hl = Helmlab()

<span class="text-[#52525b]"># Recommended: perceptual difference from hex (COMBVD-fit metric)</span>
dist = hl.<span class="text-[#34d399]">difference</span>(<span class="text-[#fb923c]">"#ff0000"</span>, <span class="text-[#fb923c]">"#00ff00"</span>)
<span class="text-[#52525b]"># &rarr; ~0.149 (saturates near 0.15 for very dissimilar pairs)</span>

<span class="text-[#52525b]"># Difference + observer-disagreement reliability (experimental)</span>
r = hl.<span class="text-[#34d399]">difference_with_confidence</span>(<span class="text-[#fb923c]">"#808080"</span>, <span class="text-[#fb923c]">"#828282"</span>)
<span class="text-[#52525b]"># &rarr; de=0.012, p_noticeable=0.08, reliability=0.41, reliable=False</span>

<span class="text-[#52525b]"># Uncompressed Euclidean Lab (fast, for quick UI checks)</span>
dist = hl.<span class="text-[#34d399]">euclidean_distance</span>(<span class="text-[#fb923c]">"#ff0000"</span>, <span class="text-[#fb923c]">"#00ff00"</span>)
<span class="text-[#52525b]"># &rarr; 1.62 — black/white &rarr; 1.12; range 0–1.6+</span>

<span class="text-[#52525b]"># Contrast ratio (WCAG-compatible)</span>
ratio = hl.<span class="text-[#34d399]">contrast_ratio</span>(<span class="text-[#fb923c]">"#ffffff"</span>, <span class="text-[#fb923c]">"#3b82f6"</span>)
<span class="text-[#52525b]"># &rarr; 3.68</span></code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} Helmlab {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#c084fc]">const</span> hl = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Helmlab</span>();

<span class="text-[#52525b]">// Euclidean Lab distance (uncompressed, hex inputs)</span>
<span class="text-[#c084fc]">const</span> dist = hl.<span class="text-[#34d399]">euclideanDistance</span>(<span class="text-[#fb923c]">'#ff0000'</span>, <span class="text-[#fb923c]">'#00ff00'</span>);  <span class="text-[#52525b]">// alias of deltaE()</span>
<span class="text-[#52525b]">// &rarr; 1.62 — black/white &rarr; 1.12; range 0–1.6+</span>

<span class="text-[#52525b]">// Compressed perceptual distance (Lab inputs, COMBVD-fit)</span>
<span class="text-[#c084fc]">const</span> lab1 = hl.<span class="text-[#34d399]">fromHex</span>(<span class="text-[#fb923c]">'#ff0000'</span>);
<span class="text-[#c084fc]">const</span> lab2 = hl.<span class="text-[#34d399]">fromHex</span>(<span class="text-[#fb923c]">'#00ff00'</span>);
<span class="text-[#c084fc]">const</span> distP = hl.<span class="text-[#34d399]">perceptualDistance</span>(lab1, lab2);

<span class="text-[#52525b]">// Contrast ratio (WCAG-compatible)</span>
<span class="text-[#c084fc]">const</span> ratio = hl.<span class="text-[#34d399]">contrastRatio</span>(<span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#fb923c]">'#3b82f6'</span>);
<span class="text-[#52525b]">// &rarr; 3.68</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">STRESS Evaluation</h3>

          <div class="for-dev">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto docs-py"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab.spaces.metric</span> <span class="text-[#c084fc]">import</span> MetricSpace

metric = MetricSpace()

<span class="text-[#52525b]"># Forward transform: XYZ &rarr; MetricSpace Lab</span>
lab = metric.<span class="text-[#34d399]">from_XYZ</span>(xyz)

<span class="text-[#52525b]"># Distance with the full enriched formula (XYZ inputs!)</span>
dist = metric.<span class="text-[#34d399]">distance</span>(xyz1, xyz2)

<span class="text-[#52525b]"># From Lab values use distance_from_lab (v0.14+)</span>
dist = metric.<span class="text-[#34d399]">distance_from_lab</span>(lab1, lab2)</code></pre>

            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto docs-js"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} MetricSpace {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#c084fc]">const</span> metric = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">MetricSpace</span>();

<span class="text-[#52525b]">// Forward transform: XYZ &rarr; MetricSpace Lab</span>
<span class="text-[#c084fc]">const</span> lab = metric.<span class="text-[#34d399]">fromXYZ</span>(xyz);

<span class="text-[#52525b]">// Distance with the full enriched formula (Lab inputs</span>
<span class="text-[#52525b]">// in JS — unlike Python, where distance() takes XYZ)</span>
<span class="text-[#c084fc]">const</span> dist = metric.<span class="text-[#34d399]">distanceFromLab</span>(lab1, lab2);</code></pre>
          </div>

          <div class="for-dev mt-6">
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4">
              <p class="text-sm text-[#a1a1aa]">
                <strong class="text-[#fafafa]">Benchmark:</strong> STRESS {claims.stress.metricSpace.combvd} on COMBVD (with Bradford CAT). Beats CIEDE2000 ({claims.stress.ciede2000.combvd}), CIE94 ({claims.stress.cie94.combvd}), CAM16-UCS ({claims.stress.cam16ucs.combvd}), CIECAM02-UCS ({claims.stress.ciecam02ucs.combvd}), and every other published Lab-only formula. Includes Helmholtz-Kohlrausch compensation, hue-dependent lightness, chroma scaling, and neutral correction.
              </p>
            </div>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- HELMLAB CLASS -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="helmlab-class" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">Helmlab Class</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              The Helmlab class is the single import you need for most tasks. It wraps both GenSpace and MetricSpace and provides a clean hex-in, hex-out API.
            </p>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              High-level API class. Accepts hex strings, handles sRGB conversion and gamut mapping internally. Uses GenSpace for generation methods and MetricSpace for distance/contrast methods.
            </p>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-8">palette()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Generate a lightness-uniform palette from a single color.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">palette = hl.<span class="text-[#34d399]">palette</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, steps=<span class="text-[#f97316]">10</span>)
<span class="text-[#52525b]"># Returns list of hex strings: ['#fafcff', '#ccdeff', ...]</span></code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> palette = hl.<span class="text-[#34d399]">palette</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#f97316]">10</span>);
<span class="text-[#52525b]">// Returns string[]: ['#fafcff', '#ccdeff', ...]</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">gradient()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Generate a perceptually uniform gradient between two colors using CIEDE2000 arc-length reparameterization.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">grad = hl.<span class="text-[#34d399]">gradient</span>(<span class="text-[#fb923c]">"#ef4444"</span>, <span class="text-[#fb923c]">"#3b82f6"</span>, steps=<span class="text-[#f97316]">16</span>)
<span class="text-[#52525b]"># Steps are equally spaced in CIEDE2000 perceptual distance</span></code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> grad = hl.<span class="text-[#34d399]">gradient</span>(<span class="text-[#fb923c]">'#ef4444'</span>, <span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#f97316]">16</span>);
<span class="text-[#52525b]">// Steps are equally spaced in CIEDE2000 perceptual distance</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">semanticScale()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Generate a Tailwind-style 50-950 semantic scale from a base color.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">scale = hl.<span class="text-[#34d399]">semantic_scale</span>(<span class="text-[#fb923c]">"#3b82f6"</span>)
<span class="text-[#52525b]"># Returns dict: {"{"}50: '#e7efff', 100: '#d4e3ff', ..., 950: '#000046'{"}"}</span></code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> scale = hl.<span class="text-[#34d399]">semanticScale</span>(<span class="text-[#fb923c]">'#3b82f6'</span>);
<span class="text-[#52525b]">// Returns Record&lt;number, string&gt;: {"{"}50: '#e7efff', 100: '#d4e3ff', ...{"}"}</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">adaptToMode()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Adapt a color between light and dark mode via soft L-inversion in GenSpace.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Light &rarr; dark</span>
dark = hl.<span class="text-[#34d399]">adapt_to_mode</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, from_mode=<span class="text-[#fb923c]">"light"</span>, to_mode=<span class="text-[#fb923c]">"dark"</span>)

<span class="text-[#52525b]"># Dark &rarr; light</span>
light = hl.<span class="text-[#34d399]">adapt_to_mode</span>(<span class="text-[#fb923c]">"#93c5fd"</span>, from_mode=<span class="text-[#fb923c]">"dark"</span>, to_mode=<span class="text-[#fb923c]">"light"</span>)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Light &rarr; dark</span>
<span class="text-[#c084fc]">const</span> dark = hl.<span class="text-[#34d399]">adaptToMode</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#fb923c]">'light'</span>, <span class="text-[#fb923c]">'dark'</span>);

<span class="text-[#52525b]">// Dark &rarr; light</span>
<span class="text-[#c084fc]">const</span> light = hl.<span class="text-[#34d399]">adaptToMode</span>(<span class="text-[#fb923c]">'#93c5fd'</span>, <span class="text-[#fb923c]">'dark'</span>, <span class="text-[#fb923c]">'light'</span>);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">ensureContrast()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Adjust a color to meet a minimum WCAG contrast ratio against a background.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Ensure WCAG AA (4.5:1) contrast</span>
adjusted = hl.<span class="text-[#34d399]">ensure_contrast</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, <span class="text-[#fb923c]">"#ffffff"</span>, min_ratio=<span class="text-[#f97316]">4.5</span>)

<span class="text-[#52525b]"># Ensure WCAG AAA (7:1) contrast</span>
adjusted = hl.<span class="text-[#34d399]">ensure_contrast</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, <span class="text-[#fb923c]">"#ffffff"</span>, min_ratio=<span class="text-[#f97316]">7.0</span>)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Ensure WCAG AA (4.5:1) contrast</span>
<span class="text-[#c084fc]">const</span> adjusted = hl.<span class="text-[#34d399]">ensureContrast</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#f97316]">4.5</span>);

<span class="text-[#52525b]">// Ensure WCAG AAA (7:1) contrast</span>
<span class="text-[#c084fc]">const</span> adjusted2 = hl.<span class="text-[#34d399]">ensureContrast</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#f97316]">7.0</span>);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">meetsContrast()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Check if a foreground/background pair meets a WCAG contrast level without modifying the colors.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">hl.<span class="text-[#34d399]">meets_contrast</span>(<span class="text-[#fb923c]">"#3b82f6"</span>, <span class="text-[#fb923c]">"#ffffff"</span>, level=<span class="text-[#fb923c]">"AA"</span>)   <span class="text-[#52525b]"># False</span>
hl.<span class="text-[#34d399]">meets_contrast</span>(<span class="text-[#fb923c]">"#1e40af"</span>, <span class="text-[#fb923c]">"#ffffff"</span>, level=<span class="text-[#fb923c]">"AAA"</span>)  <span class="text-[#52525b]"># True</span></code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">hl.<span class="text-[#34d399]">meetsContrast</span>(<span class="text-[#fb923c]">'#3b82f6'</span>, <span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#fb923c]">'AA'</span>);   <span class="text-[#52525b]">// false</span>
hl.<span class="text-[#34d399]">meetsContrast</span>(<span class="text-[#fb923c]">'#1e40af'</span>, <span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#fb923c]">'AAA'</span>);  <span class="text-[#52525b]">// true</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">adaptPair()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Adapt a foreground/background pair to a target mode while guaranteeing minimum contrast ratio.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Returns (fg_hex, bg_hex) tuple with contrast &ge; 4.5</span>
fg, bg = hl.<span class="text-[#34d399]">adapt_pair</span>(
    <span class="text-[#fb923c]">"#3366ff"</span>, <span class="text-[#fb923c]">"#ffffff"</span>,
    from_mode=<span class="text-[#fb923c]">"light"</span>, to_mode=<span class="text-[#fb923c]">"dark"</span>, min_ratio=<span class="text-[#f97316]">4.5</span>
)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Returns [fgHex, bgHex] with contrast &ge; 4.5</span>
<span class="text-[#c084fc]">const</span> [fg, bg] = hl.<span class="text-[#34d399]">adaptPair</span>(<span class="text-[#fb923c]">'#3366ff'</span>, <span class="text-[#fb923c]">'#ffffff'</span>, <span class="text-[#fb923c]">'light'</span>, <span class="text-[#fb923c]">'dark'</span>, <span class="text-[#f97316]">4.5</span>);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">info()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Get detailed color information: hex, sRGB, XYZ, Lab (metric), L/C/H, and relative luminance.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">info = hl.<span class="text-[#34d399]">info</span>(<span class="text-[#fb923c]">"#3b82f6"</span>)
<span class="text-[#52525b]"># {"{"}hex: '#3b82f6', srgb: [0.231, 0.510, 0.965],</span>
<span class="text-[#52525b]">#  xyz: [0.264, 0.235, 0.903], lab: [0.722, -0.247, -0.238],</span>
<span class="text-[#52525b]">#  L: 0.722, C: 0.343, H: 223.9, luminance: 0.235{"}"}</span></code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> info = hl.<span class="text-[#34d399]">info</span>(<span class="text-[#fb923c]">'#3b82f6'</span>);
<span class="text-[#52525b]">// {"{"}hex, srgb, xyz, lab, L, C, H, luminance{"}"}</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">perceptualDistance() / distance_from_lab()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">
            Compressed perceptual distance using MetricSpace with Minkowski exponent
            and monotonic compression — STRESS {claims.stress.metricSpace.combvd} on COMBVD vs CIEDE2000's {claims.stress.ciede2000.combvd}.
            More accurate than <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">deltaE()</code>
            which is simple Euclidean. Note: compression saturates near ~0.15 for
            very dissimilar pairs — relative ordering is preserved but absolute
            magnitudes plateau.
          </p>
          <p class="text-[#a1a1aa] mb-4 text-sm">
            ⚠ <strong>Input contract:</strong> <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">MetricSpace.distance(XYZ_1, XYZ_2)</code>
            expects <strong>CIE XYZ</strong>, not Lab. Passing Lab silently saturates near 0.15.
            For Lab inputs use <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">MetricSpace.distance_from_lab(lab_1, lab_2)</code>
            (added v{claims.version}) or the convenience wrapper <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">Helmlab.perceptual_distance(lab1, lab2)</code>.
          </p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Takes Metric Lab values (from from_hex / from_XYZ)</span>
lab1 = hl.<span class="text-[#34d399]">from_hex</span>(<span class="text-[#fb923c]">"#ff0000"</span>)
lab2 = hl.<span class="text-[#34d399]">from_hex</span>(<span class="text-[#fb923c]">"#00ff00"</span>)
dist = hl.<span class="text-[#34d399]">perceptual_distance</span>(lab1, lab2)
<span class="text-[#52525b]"># &rarr; 0.1482</span></code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Takes Metric Lab values (from fromHex)</span>
<span class="text-[#c084fc]">const</span> lab1 = hl.<span class="text-[#34d399]">fromHex</span>(<span class="text-[#fb923c]">'#ff0000'</span>);
<span class="text-[#c084fc]">const</span> lab2 = hl.<span class="text-[#34d399]">fromHex</span>(<span class="text-[#fb923c]">'#00ff00'</span>);
<span class="text-[#c084fc]">const</span> dist = hl.<span class="text-[#34d399]">perceptualDistance</span>(lab1, lab2);
<span class="text-[#52525b]">// &rarr; 0.1482</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">genFromSrgb() / genToSrgb()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Convert between sRGB [0,1] and GenSpace Lab. Use these when you need raw Lab values for generation workflows.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># sRGB &rarr; Gen Lab</span>
lab = hl.<span class="text-[#34d399]">gen_from_srgb</span>([<span class="text-[#f97316]">0.231</span>, <span class="text-[#f97316]">0.510</span>, <span class="text-[#f97316]">0.965</span>])

<span class="text-[#52525b]"># Gen Lab &rarr; sRGB</span>
rgb = hl.<span class="text-[#34d399]">gen_to_srgb</span>(lab)

<span class="text-[#52525b]"># Hex shortcuts</span>
lab = hl.<span class="text-[#34d399]">gen_from_hex</span>(<span class="text-[#fb923c]">"#3b82f6"</span>)
hex_str = hl.<span class="text-[#34d399]">gen_to_hex</span>(lab)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// sRGB &rarr; Gen Lab</span>
<span class="text-[#c084fc]">const</span> lab = hl.<span class="text-[#34d399]">genFromSrgb</span>([<span class="text-[#f97316]">0.231</span>, <span class="text-[#f97316]">0.510</span>, <span class="text-[#f97316]">0.965</span>]);

<span class="text-[#52525b]">// Gen Lab &rarr; sRGB</span>
<span class="text-[#c084fc]">const</span> rgb = hl.<span class="text-[#34d399]">genToSrgb</span>(lab);

<span class="text-[#52525b]">// Hex shortcuts</span>
<span class="text-[#c084fc]">const</span> lab2 = hl.<span class="text-[#34d399]">genFromHex</span>(<span class="text-[#fb923c]">'#3b82f6'</span>);
<span class="text-[#c084fc]">const</span> hex = hl.<span class="text-[#34d399]">genToHex</span>(lab2);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">genToLch() / genFromLch()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">
            Convert Gen Lab to/from cylindrical LCh (h in degrees, 0&ndash;360) &mdash; the same coordinates as the <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">helmgenlch</code> space on Color.js. Use for hue rotations and color harmonies. Added in v0.15.
          </p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Gen Lab &rarr; [L, C, h&deg;]</span>
lch = hl.<span class="text-[#34d399]">gen_to_lch</span>(hl.<span class="text-[#34d399]">gen_from_hex</span>(<span class="text-[#fb923c]">"#3b82f6"</span>))
<span class="text-[#52525b]"># &rarr; [0.5586, 0.2976, 263.1]</span>

<span class="text-[#52525b]"># Rotate hue 120&deg; (triadic), back to Lab &rarr; hex</span>
lch[2] = (lch[2] + <span class="text-[#f97316]">120</span>) % <span class="text-[#f97316]">360</span>
triad = hl.<span class="text-[#34d399]">gen_to_hex</span>(hl.<span class="text-[#34d399]">gen_from_lch</span>(lch))</code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Gen Lab &rarr; [L, C, h&deg;]</span>
<span class="text-[#c084fc]">const</span> lch = hl.<span class="text-[#34d399]">genToLch</span>(hl.<span class="text-[#34d399]">genFromHex</span>(<span class="text-[#fb923c]">'#3b82f6'</span>));
<span class="text-[#52525b]">// &rarr; [0.5586, 0.2976, 263.1]</span>

<span class="text-[#52525b]">// Rotate hue 120&deg; (triadic), back to Lab &rarr; hex</span>
<span class="text-[#c084fc]">const</span> triad = hl.<span class="text-[#34d399]">genToHex</span>(hl.<span class="text-[#34d399]">genFromLch</span>([lch[0], lch[1], (lch[2] + <span class="text-[#f97316]">120</span>) % <span class="text-[#f97316]">360</span>]));</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">toHexP3() / toHexRec2020()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Convert Metric Lab to wide-gamut CSS strings. Both methods perform proper gamut mapping (preserving lightness and hue, reducing chroma) so colors outside the target gamut land cleanly inside it.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">lab = hl.<span class="text-[#34d399]">from_hex</span>(<span class="text-[#fb923c]">"#ff0000"</span>)

p3_css = hl.<span class="text-[#34d399]">to_hex_p3</span>(lab)
<span class="text-[#52525b]"># &rarr; 'color(display-p3 0.9176 0.2003 0.1386)'</span>

rec_css = hl.<span class="text-[#34d399]">to_hex_rec2020</span>(lab)
<span class="text-[#52525b]"># &rarr; 'color(rec2020 0.7920 0.2310 0.0738)'</span>

<span class="text-[#52525b]"># Gamut tests</span>
hl.<span class="text-[#34d399]">is_in_srgb</span>(lab)     <span class="text-[#52525b]"># True/False</span>
hl.<span class="text-[#34d399]">is_in_p3</span>(lab)
hl.<span class="text-[#34d399]">is_in_rec2020</span>(lab)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> lab = hl.<span class="text-[#34d399]">fromHex</span>(<span class="text-[#fb923c]">'#ff0000'</span>);

<span class="text-[#c084fc]">const</span> p3Css = hl.<span class="text-[#34d399]">toHexP3</span>(lab);
<span class="text-[#52525b]">// &rarr; 'color(display-p3 0.9176 0.2003 0.1386)'</span>

<span class="text-[#c084fc]">const</span> recCss = hl.<span class="text-[#34d399]">toHexRec2020</span>(lab);
<span class="text-[#52525b]">// &rarr; 'color(rec2020 0.7920 0.2310 0.0738)'</span>

<span class="text-[#52525b]">// Gamut tests</span>
hl.<span class="text-[#34d399]">isInSrgb</span>(lab);     <span class="text-[#52525b]">// true/false</span>
hl.<span class="text-[#34d399]">isInP3</span>(lab);
hl.<span class="text-[#34d399]">isInRec2020</span>(lab);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">paletteHues()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Generate a hue ring at fixed lightness and chroma. Useful for creating categorical color schemes.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># 12 evenly-spaced hues at L=0.6, C=0.15</span>
hues = hl.<span class="text-[#34d399]">palette_hues</span>(lightness=<span class="text-[#f97316]">0.6</span>, chroma=<span class="text-[#f97316]">0.15</span>, steps=<span class="text-[#f97316]">12</span>)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// 12 evenly-spaced hues at L=0.6, C=0.15</span>
<span class="text-[#c084fc]">const</span> hues = hl.<span class="text-[#34d399]">paletteHues</span>(<span class="text-[#f97316]">0.6</span>, <span class="text-[#f97316]">0.15</span>, <span class="text-[#f97316]">12</span>);</code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">fromXYZ() / toXYZ()</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Direct CIE XYZ conversion via the MetricSpace pipeline.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># CIE XYZ &rarr; Helmlab Lab (metric pipeline)</span>
lab = hl.<span class="text-[#34d399]">from_XYZ</span>([<span class="text-[#f97316]">0.9505</span>, <span class="text-[#f97316]">1.0</span>, <span class="text-[#f97316]">1.089</span>])

<span class="text-[#52525b]"># Helmlab Lab &rarr; CIE XYZ</span>
xyz = hl.<span class="text-[#34d399]">to_XYZ</span>(lab)</code></pre>
          </div>
          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Use MetricSpace directly for XYZ access</span>
<span class="text-[#c084fc]">import</span> {"{"} MetricSpace {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;
<span class="text-[#c084fc]">const</span> metric = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">MetricSpace</span>();
<span class="text-[#c084fc]">const</span> lab = metric.<span class="text-[#34d399]">fromXYZ</span>([<span class="text-[#f97316]">0.9505</span>, <span class="text-[#f97316]">1.0</span>, <span class="text-[#f97316]">1.089</span>]);
<span class="text-[#c084fc]">const</span> xyz = metric.<span class="text-[#34d399]">toXYZ</span>(lab);</code></pre>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- COLOR.JS -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="colorjs" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">Using with Color.js</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <!-- Status callout -->
          <div class="rounded-lg border border-amber-400/[0.18] bg-amber-400/[0.04] px-4 py-3 mb-6 text-sm">
            <p class="text-[#fcd34d] font-medium mb-1">Pending next Color.js release</p>
            <p class="text-[#a1a1aa]">
              Helmlab was merged into <a href="https://github.com/color-js/color.js" target="_blank" rel="noopener noreferrer" class="text-white underline-offset-2 hover:underline">color-js/color.js</a> master in April 2026. The current published <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-xs">colorjs.io@0.6.1</code> does <strong>not</strong> include it yet — install from git or wait for the next release. Use the <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-xs">helmlab</code> package above for production today.
            </p>
            <pre class="mt-3 rounded bg-[#111115] p-3 overflow-x-auto"><code class="font-mono text-xs"><span class="text-[#52525b"># install from git until the next Color.js release ships</span>
<span class="text-[#34d399]">npm install github:color-js/color.js</span></code></pre>
          </div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              <a href="https://colorjs.io/docs/spaces.html#helmgen" target="_blank" rel="noopener noreferrer" class="text-white underline-offset-2 hover:underline">Color.js</a> is the standard library many design tools and component systems already use. Once the next release lands, you'll be able to convert any color to <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">"helmgen"</code> or <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">"helmlab-metric"</code> the same way you do with <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">"oklab"</code>.
            </p>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              Three spaces and one custom deltaE method are registered on Color.js master — same one-line API as <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">oklab</code> / <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">lch</code>:
            </p>

            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] overflow-hidden mb-6">
              <table class="w-full text-sm">
                <thead class="bg-white/[0.02] border-b border-white/[0.06]">
                  <tr>
                    <th class="text-left px-4 py-3 font-medium text-[#a1a1aa]">Space ID</th>
                    <th class="text-left px-4 py-3 font-medium text-[#a1a1aa]">Coords</th>
                    <th class="text-left px-4 py-3 font-medium text-[#a1a1aa]">Use for</th>
                  </tr>
                </thead>
                <tbody class="divide-y divide-white/[0.04]">
                  <tr>
                    <td class="px-4 py-3"><code class="text-[#fafafa] font-mono">helmgen</code></td>
                    <td class="px-4 py-3 text-[#a1a1aa] font-mono text-xs">L, a, b</td>
                    <td class="px-4 py-3 text-[#a1a1aa]">GenSpace — palettes, gradients, gamut mapping</td>
                  </tr>
                  <tr>
                    <td class="px-4 py-3"><code class="text-[#fafafa] font-mono">helmgenlch</code></td>
                    <td class="px-4 py-3 text-[#a1a1aa] font-mono text-xs">L, C, h</td>
                    <td class="px-4 py-3 text-[#a1a1aa]">GenSpace in cylindrical (LCh) form</td>
                  </tr>
                  <tr>
                    <td class="px-4 py-3"><code class="text-[#fafafa] font-mono">helmlab-metric</code></td>
                    <td class="px-4 py-3 text-[#a1a1aa] font-mono text-xs">L, a, b</td>
                    <td class="px-4 py-3 text-[#a1a1aa]">MetricSpace — perceptual distance</td>
                  </tr>
                </tbody>
              </table>
            </div>

            <p class="text-sm text-[#a1a1aa] mb-2">
              Plus a custom deltaE method <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-xs">"Helmlab"</code> implementing the v21 pair-dependent SL/SC weighting (STRESS {claims.stress.metricSpace.combvd} on COMBVD).
            </p>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> Color <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">"colorjs.io"</span>;

<span class="text-[#52525b]">// Construct directly in any helm space</span>
<span class="text-[#c084fc]">const</span> blue = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Color</span>(<span class="text-[#fb923c]">"helmgen"</span>, [<span class="text-[#f97316]">0.55</span>, <span class="text-[#f97316]">-0.04</span>, <span class="text-[#f97316]">-0.18</span>]);

<span class="text-[#52525b]">// Convert from sRGB / hex / any other space</span>
<span class="text-[#c084fc]">const</span> hg = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Color</span>(<span class="text-[#fb923c]">"#3b82f6"</span>).<span class="text-[#34d399]">to</span>(<span class="text-[#fb923c]">"helmgen"</span>);

<span class="text-[#52525b]">// Smooth gradient — uses GenSpace for interpolation</span>
<span class="text-[#c084fc]">const</span> grad = blue.<span class="text-[#34d399]">steps</span>(<span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Color</span>(<span class="text-[#fb923c]">"white"</span>), {"{"} space: <span class="text-[#fb923c]">"helmgen"</span>, steps: <span class="text-[#f97316]">32</span> {"}"});

<span class="text-[#52525b]">// Perceptual distance — pair-dependent MetricSpace deltaE (v21)</span>
<span class="text-[#c084fc]">const</span> d = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Color</span>(<span class="text-[#fb923c]">"red"</span>).<span class="text-[#34d399]">deltaE</span>(<span class="text-[#fb923c]">"green"</span>, <span class="text-[#fb923c]">"Helmlab"</span>);

<span class="text-[#52525b]">// LCh form — useful for hue rotations</span>
<span class="text-[#c084fc]">const</span> rotated = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">Color</span>(<span class="text-[#fb923c]">"helmgenlch"</span>, [<span class="text-[#f97316]">0.6</span>, <span class="text-[#f97316]">0.18</span>, <span class="text-[#f97316]">240</span>]);
rotated.<span class="text-[#34d399]">set</span>(<span class="text-[#fb923c]">"helmgenlch.h"</span>, h =&gt; h + <span class="text-[#f97316]">30</span>);</code></pre>
          </div>

          <div class="docs-py">
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4 mt-4">
              <p class="text-sm text-[#a1a1aa]">
                Color.js is JavaScript-only. For Python use the <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-xs">helmlab</code> package directly (same math, identical output).
              </p>
            </div>
          </div>

          <div class="for-dev mt-6">
            <p class="text-sm text-[#52525b]">
              The math is identical to the standalone <code class="text-[#a1a1aa] bg-white/[0.04] px-1.5 py-0.5 rounded text-xs">helmlab</code> package. Pick whichever fits your stack: helmlab if you want a tiny zero-dep bundle and the high-level helpers (palette, gradient, semanticScale), Color.js if you already use it for parsing, gamut mapping, and multi-space conversions.
            </p>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- AI SKILLS -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="ai-skills" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">AI Skills</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <p class="text-[#a1a1aa] mb-4">
            Teach your AI assistant to get color right. Two agent skills, installable into
            Claude Code, Codex, Gemini CLI or Cursor with one command:
          </p>

          <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto mb-6"><code class="font-mono text-sm">npx skills add Grkmyldz148/color-skills</code></pre>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
            <div class="rounded-xl border border-white/[0.06] bg-[#0c0c0f] p-5">
              <h3 class="text-sm font-medium text-[#fafafa] mb-1">color-space-routing</h3>
              <p class="text-xs text-[#a1a1aa]">Task &rarr; space routing with measured evidence, Lab-vs-LCh form rules, pitfalls and a graveyard of measured-dead approaches. Vendor-neutral: routes to OKLab, CIELAB, CAM16, Jzazbz or Helmlab wherever each wins. <span class="text-[#34d399]">Benchmarked: lifts claude-haiku 40%&rarr;93% and sonnet 42%&rarr;88%</span> on 20 auto-verifiable color tasks.</p>
            </div>
            <div class="rounded-xl border border-white/[0.06] bg-[#0c0c0f] p-5">
              <h3 class="text-sm font-medium text-[#fafafa] mb-1">helmlab</h3>
              <p class="text-xs text-[#a1a1aa]">Correct API usage of this library — the two-space model, verified outputs and the exact gotchas that break AI-generated code. <span class="text-[#34d399]">Benchmarked: haiku 8%&rarr;75%, sonnet 38%&rarr;92%</span> on 8 API tasks. Without it, models hallucinate the API.</p>
            </div>
          </div>

          <p class="text-xs text-[#52525b]">
            All numbers from fresh headless sessions, 3 reps per task; the harness is validated with golden answers and raw per-run outputs are committed — <a href="https://github.com/Grkmyldz148/color-skills" target="_blank" rel="noopener noreferrer" class="text-[#a1a1aa] hover:text-white underline-offset-2 hover:underline">github.com/Grkmyldz148/color-skills</a>.
          </p>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- TYPESCRIPT -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="typescript" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">TypeScript</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              If you use TypeScript, Helmlab ships complete type definitions. Your editor will autocomplete every method and parameter.
            </p>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              Full TypeScript declarations included. ESM and CJS builds. All parameter interfaces are exported for custom configurations.
            </p>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> <span class="text-[#c084fc]">type</span> {"{"} Lab, GenParams, MetricParams {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;
<span class="text-[#c084fc]">import</span> {"{"} Helmlab, GenSpace, MetricSpace {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#52525b]">// Lab is a [number, number, number] tuple</span>
<span class="text-[#c084fc]">const</span> color: <span class="text-[#60a5fa]">Lab</span> = [<span class="text-[#f97316]">0.5</span>, <span class="text-[#f97316]">-0.1</span>, <span class="text-[#f97316]">0.2</span>];

<span class="text-[#52525b]">// GenParams / MetricParams for custom spaces</span>
<span class="text-[#c084fc]">const</span> params: <span class="text-[#60a5fa]">GenParams</span> = {"{"}
  M1: [[...], [...], [...]],
  M2: [[...], [...], [...]],
  alpha: <span class="text-[#f97316]">0.021</span>,
  chroma_power: <span class="text-[#f97316]">0.978</span>,
  <span class="text-[#52525b]">// ... other parameters</span>
{"}"};</code></pre>
          </div>

          <div class="docs-py">
            <div class="rounded-lg border border-white/[0.06] bg-[#0c0c0f] p-4">
              <p class="text-sm text-[#52525b]">
                Python type hints are available via <code class="text-[#a1a1aa]">py.typed</code> marker. Works with mypy and pyright.
              </p>
            </div>
          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- ADVANCED -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="advanced" class="for-dev mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">Advanced</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-dev">
            <h3 class="text-lg font-semibold mb-4 mt-4">Custom Parameters</h3>
            <p class="text-[#a1a1aa] mb-4">
              Both GenSpace and MetricSpace accept custom parameter objects. Use this for research or when training on your own datasets.
            </p>

            <div class="docs-py">
              <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab.spaces.gen</span> <span class="text-[#c084fc]">import</span> GenSpace, GenParams

<span class="text-[#52525b]"># Load custom parameters</span>
params = GenParams.<span class="text-[#34d399]">load</span>(<span class="text-[#fb923c]">"my_params.json"</span>)

gen = GenSpace(params=params)</code></pre>
            </div>

            <div class="docs-js">
              <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} GenSpace, compileGenParams {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;
<span class="text-[#c084fc]">import</span> params <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'./my_params.json'</span>;

<span class="text-[#c084fc]">const</span> gen = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">GenSpace</span>(<span class="text-[#34d399]">compileGenParams</span>(params));</code></pre>
            </div>

            <h3 class="text-lg font-semibold mb-4 mt-10">Raw Space Access</h3>
            <p class="text-[#a1a1aa] mb-4">
              For maximum control, use GenSpace or MetricSpace directly instead of the Helmlab wrapper. This gives you access to the raw Lab coordinates without hex conversion or gamut mapping.
            </p>

            <div class="docs-py">
              <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab.spaces.gen</span> <span class="text-[#c084fc]">import</span> GenSpace
<span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab.spaces.metric</span> <span class="text-[#c084fc]">import</span> MetricSpace
<span class="text-[#c084fc]">from</span> <span class="text-[#60a5fa]">helmlab.utils.srgb_convert</span> <span class="text-[#c084fc]">import</span> sRGB_to_XYZ

gen = GenSpace()
metric = MetricSpace(neutral_correction=<span class="text-[#f97316]">True</span>)  <span class="text-[#52525b]"># NC on = Helmlab wrapper default</span>

<span class="text-[#52525b]"># Raw spaces take CIE XYZ, not sRGB</span>
xyz = <span class="text-[#34d399]">sRGB_to_XYZ</span>([<span class="text-[#f97316]">0.2</span>, <span class="text-[#f97316]">0.5</span>, <span class="text-[#f97316]">0.8</span>])
gen_lab = gen.<span class="text-[#34d399]">from_XYZ</span>(xyz)
met_lab = metric.<span class="text-[#34d399]">from_XYZ</span>(xyz)

<span class="text-[#52525b]"># These are different spaces -- different Lab values</span>
<span class="text-[#c084fc]">print</span>(gen_lab)   <span class="text-[#52525b]"># [0.518, -0.056, -0.211]</span>
<span class="text-[#c084fc]">print</span>(met_lab)   <span class="text-[#52525b]"># [0.660, -0.255, -0.199]</span></code></pre>
            </div>

            <div class="docs-js">
              <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">import</span> {"{"} GenSpace, MetricSpace, srgbToXyz,
  compileGenParams, getDefaultGenParams,
  compileParams, getDefaultParams {"}"} <span class="text-[#c084fc]">from</span> <span class="text-[#fb923c]">'helmlab'</span>;

<span class="text-[#52525b]">// Raw classes take compiled params (the Helmlab wrapper does this for you)</span>
<span class="text-[#c084fc]">const</span> gen = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">GenSpace</span>(<span class="text-[#34d399]">compileGenParams</span>(<span class="text-[#34d399]">getDefaultGenParams</span>()));
<span class="text-[#c084fc]">const</span> metric = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">MetricSpace</span>(<span class="text-[#34d399]">compileParams</span>(<span class="text-[#34d399]">getDefaultParams</span>()));  <span class="text-[#52525b]">// NC on by default</span>

<span class="text-[#52525b]">// Raw spaces take CIE XYZ, not sRGB</span>
<span class="text-[#c084fc]">const</span> xyz = <span class="text-[#34d399]">srgbToXyz</span>([<span class="text-[#f97316]">0.2</span>, <span class="text-[#f97316]">0.5</span>, <span class="text-[#f97316]">0.8</span>]);
<span class="text-[#c084fc]">const</span> genLab = gen.<span class="text-[#34d399]">fromXYZ</span>(xyz);
<span class="text-[#c084fc]">const</span> metLab = metric.<span class="text-[#34d399]">fromXYZ</span>(xyz);

<span class="text-[#52525b]">// These are different spaces -- different Lab values</span>
console.<span class="text-[#34d399]">log</span>(genLab);  <span class="text-[#52525b]">// [0.518, -0.056, -0.211]</span>
console.<span class="text-[#34d399]">log</span>(metLab);  <span class="text-[#52525b]">// [0.660, -0.255, -0.199]</span></code></pre>
            </div>

            <h3 class="text-lg font-semibold mb-4 mt-10">Neutral Correction Toggle</h3>
            <p class="text-[#a1a1aa] mb-4">
              MetricSpace includes a neutral correction (NC) stage that renders grays exactly neutral. The Helmlab wrapper (and the raw JS class) enable it by default; the raw Python class defaults to off. Toggle it explicitly:
            </p>

            <div class="docs-py">
              <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]"># Neutral correction (raw-class default: False)</span>
metric = MetricSpace(neutral_correction=<span class="text-[#f97316]">False</span>)</code></pre>
            </div>

            <div class="docs-js">
              <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#52525b]">// Neutral correction (raw-class default: true)</span>
<span class="text-[#c084fc]">const</span> metric = <span class="text-[#c084fc]">new</span> <span class="text-[#60a5fa]">MetricSpace</span>(<span class="text-[#34d399]">compileParams</span>(<span class="text-[#34d399]">getDefaultParams</span>()), {"{"} neutralCorrection: <span class="text-[#f97316]">false</span> {"}"});</code></pre>
            </div>

          </div>
        </section>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- TOKEN EXPORT -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <section id="token-export" class="mb-16 scroll-mt-24">
          <h2 class="text-2xl font-bold mb-2">Token Export</h2>
          <div class="h-px bg-white/[0.06] mb-8"></div>

          <div class="for-designer mb-6">
            <p class="text-[#a1a1aa] mb-4">
              Export your palettes directly to CSS custom properties, Tailwind configs, Swift (iOS), Android colors, or raw JSON. The <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">export()</code> method on the Helmlab class gives you a TokenExporter with all formats.
            </p>
          </div>

          <div class="for-dev mb-6">
            <p class="text-[#a1a1aa] mb-4">
              <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">hl.export()</code> returns a <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">TokenExporter</code> instance bound to the Helmlab's MetricSpace. Pass Metric Lab values (from <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">from_hex</code> / <code class="text-[#fafafa] bg-white/[0.04] px-1.5 py-0.5 rounded text-sm">from_XYZ</code>) &mdash; Gen Lab values will silently produce the wrong color. Conversions go through Metric Lab &rarr; XYZ &rarr; sRGB/P3/OKLab internally.
            </p>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-8">Single-Color Formats</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Convert a Metric Lab value to any output format.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">ex = hl.<span class="text-[#34d399]">export</span>()
lab = hl.<span class="text-[#34d399]">from_hex</span>(<span class="text-[#fb923c]">"#3b82f6"</span>)  <span class="text-[#52525b]"># Metric Lab</span>

ex.<span class="text-[#34d399]">to_css_hex</span>(lab)        <span class="text-[#52525b]"># '#3b82f6'</span>
ex.<span class="text-[#34d399]">to_css_rgb</span>(lab)        <span class="text-[#52525b]"># 'rgb(59, 130, 246)'</span>
ex.<span class="text-[#34d399]">to_css_hsl</span>(lab)        <span class="text-[#52525b]"># 'hsl(217, 91%, 60%)'</span>
ex.<span class="text-[#34d399]">to_css_oklch</span>(lab)      <span class="text-[#52525b]"># 'oklch(62.3% 0.1881 259.8)'</span>
ex.<span class="text-[#34d399]">to_css_displayp3</span>(lab)  <span class="text-[#52525b]"># 'color(display-p3 0.3047 0.5035 0.9337)'</span>
ex.<span class="text-[#34d399]">to_android_argb</span>(lab)   <span class="text-[#52525b]"># '0xFF3b82f6'</span>
ex.<span class="text-[#34d399]">to_ios_p3</span>(lab)         <span class="text-[#52525b]"># {"{"}r: 0.3047, g: 0.5035, b: 0.9337{"}"}</span>
ex.<span class="text-[#34d399]">to_swift_literal</span>(lab)  <span class="text-[#52525b]"># 'Color(.displayP3, red: 0.3047, ...)'</span></code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> ex = hl.<span class="text-[#34d399]">export</span>();
<span class="text-[#c084fc]">const</span> lab = hl.<span class="text-[#34d399]">fromHex</span>(<span class="text-[#fb923c]">'#3b82f6'</span>);  <span class="text-[#52525b]">// Metric Lab</span>

ex.<span class="text-[#34d399]">toCssHex</span>(lab);        <span class="text-[#52525b]">// '#3b82f6'</span>
ex.<span class="text-[#34d399]">toCssRgb</span>(lab);        <span class="text-[#52525b]">// 'rgb(59, 130, 246)'</span>
ex.<span class="text-[#34d399]">toCssHsl</span>(lab);        <span class="text-[#52525b]">// 'hsl(217, 91%, 60%)'</span>
ex.<span class="text-[#34d399]">toCssOklch</span>(lab);      <span class="text-[#52525b]">// 'oklch(62.3% 0.1881 259.8)'</span>
ex.<span class="text-[#34d399]">toCssDisplayP3</span>(lab);  <span class="text-[#52525b]">// 'color(display-p3 0.3047 0.5035 0.9337)'</span>
ex.<span class="text-[#34d399]">toAndroidArgb</span>(lab);   <span class="text-[#52525b]">// '0xFF3b82f6'</span>
ex.<span class="text-[#34d399]">toIosP3</span>(lab);         <span class="text-[#52525b]">// {"{"}r: 0.3047, g: 0.5035, b: 0.9337{"}"}</span>
ex.<span class="text-[#34d399]">toSwiftLiteral</span>(lab);  <span class="text-[#52525b]">// 'Color(.displayP3, red: 0.3047, ...)'</span></code></pre>
          </div>

          <h3 class="text-lg font-semibold mb-4 mt-10">Scale Export</h3>
          <p class="text-[#a1a1aa] mb-4 text-sm">Export an entire scale to CSS custom properties, Tailwind config, or JSON.</p>

          <div class="docs-py">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm">scale = hl.<span class="text-[#34d399]">semantic_scale</span>(<span class="text-[#fb923c]">"#3b82f6"</span>)

<span class="text-[#52525b]"># CSS custom properties</span>
css = ex.<span class="text-[#34d399]">export_css_custom_properties</span>(scale, prefix=<span class="text-[#fb923c]">"--primary"</span>)
<span class="text-[#52525b]">#   --primary-50: #e7efff;</span>
<span class="text-[#52525b]">#   --primary-100: #d4e3ff;</span>
<span class="text-[#52525b]">#   ...</span>

<span class="text-[#52525b]"># Tailwind config dict</span>
tw = ex.<span class="text-[#34d399]">export_tailwind</span>(scale, name=<span class="text-[#fb923c]">"primary"</span>)
<span class="text-[#52525b]"># {"{"}'primary': {"{"}'50': '#e7efff', '100': '#d4e3ff', ...{"}"}{"}"}</span>

<span class="text-[#52525b]"># JSON string (multiple scales)</span>
json_str = ex.<span class="text-[#34d399]">export_json</span>({"{"}<span class="text-[#fb923c]">"primary"</span>: scale, <span class="text-[#fb923c]">"accent"</span>: accent_scale{"}"})</code></pre>
          </div>

          <div class="docs-js">
            <pre class="rounded-lg bg-[#111115] p-4 overflow-x-auto"><code class="font-mono text-sm"><span class="text-[#c084fc]">const</span> scale = hl.<span class="text-[#34d399]">semanticScale</span>(<span class="text-[#fb923c]">'#3b82f6'</span>);

<span class="text-[#52525b]">// CSS custom properties</span>
<span class="text-[#c084fc]">const</span> css = ex.<span class="text-[#34d399]">exportCssCustomProperties</span>(scale, <span class="text-[#fb923c]">'--primary'</span>);
<span class="text-[#52525b]">//   --primary-50: #e7efff;</span>
<span class="text-[#52525b]">//   --primary-100: #d4e3ff;</span>
<span class="text-[#52525b]">//   ...</span>

<span class="text-[#52525b]">// Tailwind config</span>
<span class="text-[#c084fc]">const</span> tw = ex.<span class="text-[#34d399]">exportTailwind</span>(scale, <span class="text-[#fb923c]">'primary'</span>);
<span class="text-[#52525b]">// {"{"}primary: {"{"}'50': '#e7efff', '100': '#d4e3ff', ...{"}"}{"}"}</span>

<span class="text-[#52525b]">// JSON string (multiple scales)</span>
<span class="text-[#c084fc]">const</span> json = ex.<span class="text-[#34d399]">exportJson</span>({"{"} primary: scale, accent: accentScale {"}"});</code></pre>
          </div>
        </section>

        <!-- Bottom nav -->
        <div class="flex items-center justify-between pt-8 border-t border-white/[0.06]">
          <a href="/" class="text-sm text-[#a1a1aa] hover:text-white transition-colors">&larr; Home</a>
          <a href="https://github.com/Grkmyldz148/helmlab" target="_blank" rel="noopener noreferrer" class="text-sm text-[#a1a1aa] hover:text-white transition-colors">
            View on GitHub &rarr;
          </a>
        </div>
      </div>
    </div>
  </div>

  <script is:inline>
    // Language tab switcher
    var radios = document.querySelectorAll('input[name="docs-lang"]');

    function updateLangVisibility(lang) {
      document.querySelectorAll('.docs-py').forEach(function(el) {
        el.style.display = lang === 'python' ? '' : 'none';
      });
      document.querySelectorAll('.docs-js').forEach(function(el) {
        el.style.display = lang === 'js' ? '' : 'none';
      });
    }

    var checked = document.querySelector('input[name="docs-lang"]:checked');
    if (checked) updateLangVisibility(checked.value);

    radios.forEach(function(radio) {
      radio.addEventListener('change', function(e) {
        updateLangVisibility(e.target.value);
      });
    });

    // Highlight current section in sidebar (scroll-based, not IntersectionObserver)
    var sections = Array.from(document.querySelectorAll('section[id]'));
    var sidebarLinks = document.querySelectorAll('aside nav a[href^="#"]');
    var currentActive = null;

    function setActive(id) {
      if (currentActive === id) return;
      currentActive = id;
      sidebarLinks.forEach(function(link) {
        link.style.color = '';
        link.style.backgroundColor = '';
        link.style.fontWeight = '';
        link.style.borderRadius = '';
      });
      // Apply to ALL matching links (mobile + desktop sidebars)
      var activeLinks = document.querySelectorAll('aside nav a[href="#' + id + '"]');
      activeLinks.forEach(function(activeLink) {
        activeLink.style.color = '#fafafa';
        activeLink.style.backgroundColor = 'rgba(255,255,255,0.06)';
        activeLink.style.fontWeight = '500';
        activeLink.style.borderRadius = '6px';
      });
    }

    function updateActiveOnScroll() {
      var offset = 100;
      var current = sections[0];
      var positions = [];
      for (var i = 0; i < sections.length; i++) {
        var rect = sections[i].getBoundingClientRect();
        positions.push({ id: sections[i].id, top: Math.round(rect.top) });
        if (rect.top <= offset) {
          current = sections[i];
        } else {
          break;
        }
      }
      if (current) setActive(current.id);
    }

    // Initial state
    if (window.location.hash) {
      setActive(window.location.hash.slice(1));
      setTimeout(function() {
        updateActiveOnScroll();
      }, 200);
    } else {
      updateActiveOnScroll();
    }

    // Update on scroll (throttled with requestAnimationFrame)
    var ticking = false;
    window.addEventListener('scroll', function() {
      if (!ticking) {
        window.requestAnimationFrame(function() {
          updateActiveOnScroll();
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });

    // Also update when clicking sidebar links
    sidebarLinks.forEach(function(link) {
      link.addEventListener('click', function(e) {
        var href = link.getAttribute('href');
        if (href && href.startsWith('#')) {
          setActive(href.slice(1));
          setTimeout(updateActiveOnScroll, 500);
        }
      });
    });
  </script>

  <!-- Client-side docs search (no deps): indexes section headings + method names -->
  <script is:inline>
    (function () {
      const input = document.getElementById('docs-search');
      const out = document.getElementById('docs-search-results');
      if (!input || !out) return;
      const index = [];
      document.querySelectorAll('section[id]').forEach((sec) => {
        const sectionLabel = (sec.querySelector('h2')?.textContent || sec.id).trim();
        sec.querySelectorAll('h2, h3').forEach((h) => {
          if (!h.id) {
            h.id = (sec.id + '-' + (h.textContent || '').trim().toLowerCase()
              .replace(/[^a-z0-9]+/g, '-')).replace(/-+$/, '');
          }
          const title = (h.textContent || '').trim();
          if (title) index.push({ title, section: sectionLabel, id: h.id });
        });
      });
      function render(items) {
        if (!items.length) {
          out.innerHTML = '<div class="px-3 py-2 text-sm text-[#52525b]">No matches</div>';
        } else {
          out.innerHTML = items.slice(0, 12).map((it) =>
            '<a href="#' + it.id + '" class="block rounded px-3 py-1.5 text-sm text-[#a1a1aa] hover:bg-white/[0.06] hover:text-white">' +
            it.title + '<span class="block text-[10px] text-[#52525b]">' + it.section + '</span></a>'
          ).join('');
        }
        out.classList.remove('hidden');
      }
      input.addEventListener('input', () => {
        const q = input.value.trim().toLowerCase();
        if (!q) { out.classList.add('hidden'); return; }
        render(index.filter((it) =>
          it.title.toLowerCase().includes(q) || it.section.toLowerCase().includes(q)));
      });
      out.addEventListener('click', () => { out.classList.add('hidden'); input.value = ''; });
      input.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') { input.value = ''; out.classList.add('hidden'); input.blur(); }
        if (e.key === 'Enter') { const a = out.querySelector('a'); if (a) a.click(); }
      });
      document.addEventListener('keydown', (e) => {
        if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') { e.preventDefault(); input.focus(); }
      });
      document.addEventListener('click', (e) => {
        if (e.target !== input && !out.contains(e.target)) out.classList.add('hidden');
      });
    })();
  </script>
</BaseLayout>
