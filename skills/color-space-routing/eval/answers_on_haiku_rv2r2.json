{
 "T1-blue-gradient": "hl.gradient('#0000ff', '#ffffff', 16)",
 "T2-delta-e": "(hex1, hex2) => hl.difference(hex1, hex2)",
 "T3-gray-axis": "`const f = (hex) => hl.genFromHex(hex);`",
 "T4-physical-mix": "function f(a,b){const l=v=>v<=.04045?v/12.92:Math.pow((v+.055)/1.055,2.4),s=v=>v<=.0031308?v*12.92:1.055*Math.pow(v,1/2.4)-.055,p=h=>[parseInt(h.slice(1,3),16)/255,parseInt(h.slice(3,5),16)/255,parseInt(h.slice(5,7),16)/255].map(l),[ra,ga,ba]=p(a),[rb,gb,bb]=p(b);return'#'+[(ra+rb)/2,(ga+gb)/2,(ba+bb)/2].map(s).map(v=>Math.round(v*255)).map(x=>x.toString(16).padStart(2,'0')).join('')}",
 "T5-hue-wrap": "(h1, h2, t) => ((h1 + ((h2 - h1 + 540) % 360 - 180) * t + 360) % 360)",
 "T6-lch-distance": "(lch1, lch2) => Math.sqrt(Math.pow(lch1[0]-lch2[0],2)+Math.pow(lch1[1]*Math.cos(lch1[2]*Math.PI/180)-lch2[1]*Math.cos(lch2[2]*Math.PI/180),2)+Math.pow(lch1[1]*Math.sin(lch1[2]*Math.PI/180)-lch2[1]*Math.sin(lch2[2]*Math.PI/180),2))",
 "T7-cvd": "OKLab",
 "T8-picker": "okhsl",
 "T9-hdr": "Jzazbz",
 "T10-noticeable": "No — ΔE00 = 1.0 is below the ~2.3 noticeable threshold, and for low-chroma grays this is within the human noise band where CIEDE2000 itself is unreliable.",
 "T11-css-nearby": "`oklch`",
 "T12-css-distant": "`oklab`",
 "T13-wcag": "const f = (hexFg, hexBg) => { const lum = hex => { const [r, g, b] = [16, 8, 0].map(i => { const cs = ((parseInt(hex.replace('#', ''), 16) >> i) & 255) / 255; return cs <= 0.03928 ? cs / 12.92 : Math.pow((cs + 0.055) / 1.055, 2.4); }); return 0.2126 * r + 0.7152 * g + 0.0722 * b; }; const [l1, l2] = [lum(hexFg), lum(hexBg)].sort((a, b) => b - a); return (l1 + 0.05) / (l2 + 0.05); };",
 "T14-darken": "f = hex => hl.genToHex(hl.genFromLch((([L, C, h]) => [Math.max(0, L - 0.2), C, h])(hl.genToLch(hl.genFromHex(hex)))));",
 "T15-lightness-sort": "hexArray => hexArray.sort((a, b) => hl.genFromHex(a)[0] - hl.genFromHex(b)[0])",
 "T16-circular-mean": "(a, b) => (Math.atan2(Math.sin(a * Math.PI / 180) + Math.sin(b * Math.PI / 180), Math.cos(a * Math.PI / 180) + Math.cos(b * Math.PI / 180)) * 180 / Math.PI + 360) % 360",
 "T17-equal-gray": "hl.genToHex(hl.genFromLch([hl.genToLch(hl.genFromHex('#3b82f6'))[0], 0, 0]))",
 "T18-angular-dist": "(h1, h2) => Math.min(Math.abs(h1 - h2), 360 - Math.abs(h1 - h2))",
 "T19-cat": "Chromatic adaptation",
 "T20-hsv-contrast": "No — HSV Value is not perceptually uniform (yellow vs blue at identical V differ wildly in perceived brightness), making it unsuitable for accessibility contrast which requires true perceptual lightness; use OKLab's L or CIELAB's L instead."
}