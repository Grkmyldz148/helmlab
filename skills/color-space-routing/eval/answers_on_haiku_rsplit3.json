{
 "T1-blue-gradient": "Array.from({length: 16}, (_, i) => {const c1 = hl.genFromHex('#0000ff'), c2 = hl.genFromHex('#ffffff'), t = i / 15; return hl.genToHex([c1[0] + (c2[0] - c1[0]) * t, c1[1] + (c2[1] - c1[1]) * t, c1[2] + (c2[2] - c1[2]) * t]);})",
 "T2-delta-e": "(hex1, hex2) => hl.difference(hex1, hex2)",
 "T3-gray-axis": "hex => hl.genFromHex(hex)",
 "T4-physical-mix": "(a,b)=>{const d=x=>{const v=x.slice(1);return[0,2,4].map(i=>parseInt(v[i]+v[i+1],16)/255).map(c=>c<=.04045?c/12.92:Math.pow((c+.055)/1.055,2.4))},e=v=>v<=.0031308?12.92*v:1.055*Math.pow(v,1/2.4)-.055,h=(r,g,b)=>'#'+[r,g,b].map(v=>Math.round(Math.max(0,Math.min(1,e(v)))*255).toString(16).padStart(2,'0')).join('');const[r1,g1,b1]=d(a),[r2,g2,b2]=d(b);return h((r1+r2)/2,(g1+g2)/2,(b1+b2)/2)}",
 "T5-hue-wrap": "function f(h1,h2,t){const a=((h1%360)+360)%360,b=((h2%360)+360)%360,d=b-a,adj=Math.abs(d)>180?d-Math.sign(d)*360:d;return((a+t*adj)%360+360)%360}",
 "T6-lch-distance": "((l1,c1,h1,l2,c2,h2)=>Math.sqrt((l1-l2)**2 + (c1*Math.cos(h1*Math.PI/180)-c2*Math.cos(h2*Math.PI/180))**2 + (c1*Math.sin(h1*Math.PI/180)-c2*Math.sin(h2*Math.PI/180))**2))(0.5,0.1,10,0.5,0.1,350)",
 "T7-cvd": "OKLab",
 "T8-picker": "okhsl",
 "T9-hdr": "Jzazbz",
 "T10-noticeable": "No — ΔE00 1.0 is well below the 2.3 noticeable-difference threshold; low-chroma grays are perceptually indistinguishable at that delta.",
 "T11-css-nearby": "`oklch`",
 "T12-css-distant": "`oklab`\n\nRectangular form avoids hue sweep on distant endpoints; LCh would interpolate through every hue between red and blue.",
 "T13-wcag": "const f=(a,b)=>{const g=x=>{const c=x.slice(1).match(/.{2}/g).map(y=>parseInt(y,16)/255).map(z=>z<=.03928?z/12.92:((z+.055)/1.055)**2.4);return.2126*c[0]+.7152*c[1]+.0722*c[2]};const l=[g(a),g(b)];return(Math.max(...l)+.05)/(Math.min(...l)+.05)};",
 "T14-darken": "(hex) => ((lch) => hl.genToHex(hl.genFromLch({...lch, l: lch.l * 0.7})))(hl.genToLch(hl.genFromHex(hex)))",
 "T15-lightness-sort": "(hexArray) => hexArray.sort((a, b) => hl.genFromHex(a).l - hl.genFromHex(b).l)",
 "T16-circular-mean": "(h1, h2) => (r => r < 0 ? r + 360 : r)(Math.atan2((Math.sin(h1 * Math.PI / 180) + Math.sin(h2 * Math.PI / 180)) / 2, (Math.cos(h1 * Math.PI / 180) + Math.cos(h2 * Math.PI / 180)) / 2) * 180 / Math.PI)",
 "T17-equal-gray": "hl.genToHex({ L: hl.genFromHex('#3b82f6').L, a: 0, b: 0 })",
 "T18-angular-dist": "Math.min(Math.abs(20 - 340), 360 - Math.abs(20 - 340))",
 "T19-cat": "Chromatic adaptation.",
 "T20-hsv-contrast": "No. HSV Value is not perceptually uniform (yellow vs blue at same V differ wildly) — use WCAG luminance or perceptual L instead."
}