{
 "T1-blue-gradient": "hl.gradient('#0000ff', '#ffffff', 16)",
 "T2-delta-e": "(hex1, hex2) => hl.difference(hex1, hex2)",
 "T3-gray-axis": "`(hex) => hl.genFromHex(hex)`",
 "T4-physical-mix": "const f=(a,b)=>{const lin=c=>c<=.04045?c/12.92:Math.pow((c+.055)/1.055,2.4),gam=c=>c<=.0031308?12.92*c:1.055*Math.pow(c,1/2.4)-.055,parse=h=>{const r=parseInt(h.slice(1,3),16)/255,g=parseInt(h.slice(3,5),16)/255,b=parseInt(h.slice(5,7),16)/255;return[lin(r),lin(g),lin(b)]},[r1,g1,b1]=parse(a),[r2,g2,b2]=parse(b),r=gam((r1+r2)/2),g=gam((g1+g2)/2),b=gam((b1+b2)/2),hex=v=>Math.round(v*255).toString(16).padStart(2,'0');return'#'+hex(r)+hex(g)+hex(b)}",
 "T5-hue-wrap": "(h1, h2, t) => ((h1 + ((h2 - h1 + 540) % 360 - 180) * t) % 360 + 360) % 360",
 "T6-lch-distance": "(c1, c2) => Math.sqrt((c2[0]-c1[0])**2 + (c2[1]*Math.cos(c2[2]*Math.PI/180) - c1[1]*Math.cos(c1[2]*Math.PI/180))**2 + (c2[1]*Math.sin(c2[2]*Math.PI/180) - c1[1]*Math.sin(c1[2]*Math.PI/180))**2)",
 "T7-cvd": "OKLab",
 "T8-picker": "okhsl",
 "T9-hdr": "Jzazbz.",
 "T10-noticeable": "No — ΔE00 = 1.0 is below the 2.3 perceptual threshold, and near-achromatic differences are where ΔE is least trustworthy anyway.",
 "T11-css-nearby": "`oklch`",
 "T12-css-distant": "`oklab`",
 "T13-wcag": "const f=(h1,h2)=>{const getLum=h=>{const[r,g,b]=[parseInt(h.slice(1),16)>>16&255,parseInt(h.slice(1),16)>>8&255,parseInt(h.slice(1),16)&255].map(c=>c/255).map(c=>c<=0.03928?c/12.92:Math.pow((c+0.055)/1.055,2.4));return 0.2126*r+0.7152*g+0.0722*b};const[l1,l2]=[getLum(h1),getLum(h2)].sort((a,b)=>b-a);return(l1+0.05)/(l2+0.05)};",
 "T14-darken": "const f = hex => hl.genToHex(hl.genFromLch(((lch) => [Math.max(0, lch[0] - 0.2), lch[1], lch[2]])(hl.genToLch(hl.genFromHex(hex)))))",
 "T15-lightness-sort": "(hexArray) => hexArray.sort((a, b) => hl.genToLch(hl.genFromHex(a))[0] - hl.genToLch(hl.genFromHex(b))[0])",
 "T16-circular-mean": "(a, b) => ((Math.atan2(Math.sin(a * Math.PI / 180) + Math.sin(b * Math.PI / 180), Math.cos(a * Math.PI / 180) + Math.cos(b * Math.PI / 180)) * 180 / Math.PI) + 360) % 360",
 "T17-equal-gray": "`hl.genToHex([hl.genFromHex('#3b82f6')[0], 0, 0])`",
 "T18-angular-dist": "Math.min(Math.abs(340 - 20), 360 - Math.abs(340 - 20))",
 "T19-cat": "Chromatic adaptation.",
 "T20-hsv-contrast": "No — HSV Value is not perceptually uniform; yellow and blue at the same V look dramatically different in brightness, making contrast checks unreliable. Use CIELAB, OKLCH, or Helmlab lightness instead."
}